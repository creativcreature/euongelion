# F-158: Finished-state preview, the 7am flip, and an admin backend

## Metadata

- Feature ID: F-158
- Category: Platform
- Owner: founder
- Status: wip
- Decision: SA-114
- Surfaces: `/admin` (hub), `/admin/preview/daily-bread`, `/admin/edition`
  (edit-in-place), `/settings` (ADMIN card), `src/components/edition/EditionPage.tsx`,
  `src/lib/edition/deadline.ts`, `.github/workflows/monday-release.yml`,
  `.github/workflows/weekly-series.yml` (browser preview step)

## Problem

Founder:

> "I need to be able to preview both the next days Daily Bread, as well as
> Next weeks devotional as browser drafts, meaning in the 'finished state' on
> the browser so I can see exactly what it will look like — but with approve
> and reject still around each piece needing approval. I want a link in my
> settings to all the admin backend stuff. Go ahead and give me a backend so
> I can easily make edits myself."

Plus two constraints: "only I should be able to see drafts", and the go-live
rule — "the new daily bread goes live at 7 everyday. the weekly devotional
goes live 7 am monday." Silence publishes; rejection is the veto.

Before this feature: drafts were JSON blobs in a queue, the paper could not be
seen before it printed, `/admin` was a mockup with fabricated numbers, and
nothing enforced a go-live boundary at all — the paper flipped at midnight UTC
(8pm ET), which is not when a morning paper arrives.

## Solution

**One component renders the paper twice.** The whole edition was extracted
into `src/components/edition/EditionPage.tsx`; `/daily-bread` renders it live
(published-only reads) and `/admin/preview/daily-bread` renders it with
`preview` — same component, so the preview can never drift from what readers
see. Preview wraps reviewed items (lead, strip, practice) in approve/reject
chrome posting to the existing admin verdict API.

**The 7am flip** (`src/lib/edition/deadline.ts`): `effectiveEditionDate()`
computes the current edition in America/New_York — before 7am ET the site
still serves yesterday's paper; at 7am the new edition prints, and unrejected
drafts print with it (`draftIsLive`, `getLiveEdition`). Enforced at read time,
DST-safe, no cron.

**Monday 7am series release** (`.github/workflows/monday-release.yml`): dual
UTC crons + an ET-hour guard so exactly one fires at 7am ET Monday; merges the
open `reading gate` PR on `series/auto-*` (founder's veto = closing the PR)
and deploys, gated loudly on `CLOUDFLARE_API_TOKEN`.

**A real backend**: `/admin` is an honest hub (live draft counts, visible
failure states, mockups labeled NOT YET WIRED); `/api/admin/edition` gains
PATCH for in-place edits; the queue gets an Edit button (prose fields for
authored leads, JSON for the rest); Settings shows an ADMIN card only when the
admin probe returns 200. Drafts remain double-gated behind
ADMIN_EMAIL_ALLOWLIST — anonymous and non-admin users get 404.

**Redletter override**: a DB `redletter` row (once the blocked SQL lands)
overrides the page-side generator's pick, so an audited correction can replace
the computed saying without a deploy.

**Weekly series browser preview**: the Wednesday build uploads a preview
version of the Worker and comments the URL on the reading-gate PR, so next
week's devotional is readable in a real browser before Monday.

## Contracts

### Desktop

- `/admin/preview/daily-bread?date=YYYY-MM-DD` renders tomorrow's paper by
  default in finished state, with a datebar (prev/next) and per-item
  approve/reject chrome on reviewed kinds; deterministic modules render
  exactly as they will print.
- `/admin` shows the edition queue count from the live store, links to
  preview, queue, next-series override, and the GitHub reading-gate PR list.
- `/admin/edition` cards carry Edit: authored leads as prose fields
  (title/standfirst/body/pull-quotes), all other kinds as validated JSON;
  Save PATCHes and re-renders in place; server errors render verbatim.
- Settings surfaces an ADMIN card linking `/admin` only for the admin.

### Mobile

- The preview page is the same responsive EditionPage the paper uses; chrome
  bars stack above/below items and stay within the container at 375px.
- Approve/reject buttons are ≥44px touch targets.
- The admin hub and queue editor remain usable at 375px (single column).
- The crossword prints its clues beside the grid on desktop (the blank-half
  fix); mobile collapses them behind a class-driven toggle, never a
  `<details>` that leaves half the compartment empty.
- The preview page carries a fixed command bar: edition date, prev/next
  day, a live count of pieces awaiting a verdict, and REVIEW NEXT jumping
  between them; reviewable pieces are framed NEEDS YOUR EYE in plain
  English. The funnies placeholder fills its compartment (no dead zone).
- The NEXT SERIES steer box renders on /admin/edition (id `next-series`, the
  hub's anchor target) in both queue and empty states, wired to
  `/api/admin/edition/thematic` (GET current, POST set, DELETE clear) — and is
  test-pinned after a parallel rewrite erased the uncommitted original.

## Go-live rules (the founder's words)

- "the new daily bread goes live at 7 everyday" — `effectiveEditionDate()`.
- "the weekly devotional goes live 7 am monday" — monday-release.yml.
- "drafts go live if I dont directly approve" — rejection is the veto;
  approve is optional confidence.
- "only I should be able to see drafts" — preview route re-asserts the
  allowlist and 404s for everyone else; live route reads published-only plus
  drafts only once their edition date is live.
- Layout gates are NOT trusted alone: React renders layout and page
  concurrently, so `/admin` page markup could stream before the layout's
  `notFound()` aborted (measured in the Workers runtime). Every admin page
  now calls `assertAdminOr404()` itself (`src/lib/admin/assert-admin.ts`).
  Residual, accepted: Next resolves page `metadata` separately, so an
  anonymous request still sees the surface's title (e.g. "Edition Queue") on
  a 404-boundary body — surface names only, never content.

## The daily arrangement

- Same modules, different layout daily: seven hand-composed weekday
  variants in `src/lib/edition/arrange.ts`; anchors = lead (top), funnies
  (pinned index), reading (bottom); game rows never adjacent (test-pinned
  in `edition-arrange.test.ts`).

## Echo & Dust (the funnies, real)

- Canon: `content/strip-reference/ECHO-AND-DUST-CANON.md` (locked from the
  founder's 2026-08-20 brief; deviations noted in-file, e.g. v1 letters
  dialogue in-model behind an inspection gate).
- Strip No. 1 + character sheet generated (codex image_generation,
  subscription); masters in `content/strip-reference/workshop/`; the
  page asset in `public/images/edition/strip/`; DRAFT row 2026-08-21.
- Approval of the sheet + strip No. 1 gates the daily automation.
- StripPanel retitled Echo & Dust (Ninety-Nine label was stale).
- /daily-bread revalidates every 5 minutes so the 7am flip is punctual
  (hourly ISR could serve yesterday's paper until 7:59).
- The footer legal line carries the app version from package.json on every
  page (test-pinned in footer-version.test.tsx); all 47 routes swept.
- v0.8.11 / SW v142: cache generation bumped so installed PWAs swap to
  the new shell (founder saw the stale cached paper).
- Strip canon amended per founder art direction (modern day, site riso
  palette, hood-up void-faced Echo, Teddy with a real nose, Dust in warm-up
  - trucker hat + sneakers); sheet v3 / strip v4 installed on the draft row.

## The pitch site (2026-08-20)

- Demo mode: --mode=demo renders the pitch HTML in a sandboxed iframe
  (scripts run — working features reviewable). SHOW-don't-describe is law
  in the pitch skill.
- Lead plate generated daily via codex (generate-lead-art.mjs + manifest;
  page chain: generated > Vasari pick > series art).

- /admin/pitches + /admin/pitches/[slug] + /api/admin/pitches; private
  `pitches` bucket; one object per pitch (concurrent-safe, no index file);
  publisher scripts/pitches/publish-pitch.mjs; repo skill `pitch` is the
  cross-session hook; CLAUDE.md bans artifacts for pitches.

## MACHINE PAUSED (founder, 2026-08-20)

- "Before making anymore comics - we have to nail the writing, tone,
  scenarios... your cartoons have been terrible." Daily generation is OFF
  (repo var STRIP_MACHINE, default paused) until the founder approves a
  rewritten voice. The drawn pipeline itself is sound; the WRITING is the
  open problem — part of the Daily Bread brainstorm.

## Strip continuity law + gutter rhythm (2026-08-20)

- Panel continuity is canon LAW (characters AND objects hold across
  panels); generator prompt carries it; The Microwave Minute replaces the
  premiere after the founder caught Teddy vanishing mid-strip.
- The sheet's outer inset = the 6px inter-compartment gap.

## Desk band + sheet gutters (2026-08-20, follow-up)

- `.paper-sheet .edition-band` un-stripped (full compartment, center
  divider, mobile stack); `.paper-sheet` gains max-width 78rem + clamped
  padding-inline — no compartment ever flush to the viewport edge.

## The paper's full audio (2026-08-20)

- assemble-paper-narration.mts + render-paper-audio.sh (LOCAL: Voicebox)
  -> Storage m4a + manifest; EditionPage offers the track only when the
  day's manifest entry exists. Cloud rendering impossible under the
  Voicebox-only voice ruling — tradeoff surfaced to the founder.

## Wave two — daily engines (2026-08-20)

- Daily lead art: `src/lib/edition/lead-art.ts` scores the Vasari print
  pool against the day's words (manifest-first; null keeps series art;
  gallery exclusion; deterministic per date).
- Daily guides: `compose-guides-claude.mjs` (3 articles/day, drafts,
  Storage log), `/guides/daily/[date]` full reads, paper prefers DB rows.
- The Coloring Corner: `coloring-bank.ts` + `ColoringClient` (crayon
  scribble fills, dropper, persistence), appended to every arrangement.
- Daily strip machine: `generate-strip.mjs` + daily-edition.yml step
  (write→draw→inspect→install), gated on both subscription secrets.

## Wave one of the morning critique (2026-08-20)

- Strip full-format + plane-break; solid compartments; red letters red;
  found-word red circles; puzzle persistence (localStorage per puzzle);
  quiz explainers; display heads with color; /daily-bread/archive (+
  [date]); daily Echo & Dust generator wired into daily-edition.yml.

## Methodology References

- M00 (unified methodology: honest surfaces, no fabricated data, visible
  failure states, founder-gated publishing)

## Addendum — 2026-08-20: the lab (no-iframe ruling)

Founder: "stop embedding pitches in iframes and such, they need to
actually exist in the page as actual content on page... I need to see the
designs in literal context."

**Contract:** interactive pitch material is never simulated. It is built
as a real admin-gated route under `/admin/lab/<experiment>` using the
site's actual shell, stylesheet, and (where possible) real content and
data, deployed, and linked from the pitch. The pitch page renders all
pitches inline (no iframe branch); `--mode=demo` is deprecated.

### Desktop contract

- `/admin/lab/scroll-hole` renders today's real EditionPage followed by
  the proposed well as full-width `.paper-sheet` compartments; sheets
  print on approach (IntersectionObserver, 600px rootMargin).
- `/admin/lab/practices` renders eight interactive `.paper-box`
  compartments in the paper grid; margin notes use a two-column layout.

### Mobile contract

- Both lab routes are the site's own responsive compartments — no
  phone-frame chrome; single-column stacking comes from the real
  stylesheet, and all controls keep ≥44px touch targets.

### Verification

- Anonymous requests to both lab routes return 404 (page-level
  `assertAdminOr404`).
- `tsc --noEmit` clean; pitch page contains no `<iframe>`.

**Release:** v0.8.18 (SW v150) carries the lab routes and the inline-only pitch page.

## Addendum — 2026-08-24: dailiness backstop + puzzle mobile contracts

### Desktop contract

- Crossword clue lists render expanded beside the grid (unchanged); the
  mobile toggle stays hidden at ≥900px.

### Mobile contract

- Crossword: clue lists are VISIBLE by default at every width; the
  toggle reads "Hide clues" when open, "All clues" when collapsed, and
  collapses class-driven without unmounting.
- Word search: below 420px the grid's 34px cell floor lifts
  (`minmax(0,1fr)`), the board fits the viewport with no inner
  horizontal scroll, and the found-word circle overlay scales with the
  cells (cell-unit viewBox).

### Automation contract

- `daily-gapfill.yml` runs 08:00 UTC daily: for today+2 forward days it
  generates any missing guides, lead plate, and (when `STRIP_MACHINE` is
  `enabled`) strip. Generators skip existing dates; the weekly build and
  the gapfill can never double-write.

### Verification

- `crossword-clues-visible.test.tsx` pins open-by-default;
  `wordsearch-mobile-fit.test.ts` pins the narrow-viewport CSS block.
- `node --check scripts/edition/strip/generate-strip.mjs` passes (the
  split-template SyntaxError is fixed and drew four strips).

**Release:** v0.8.19 (SW v152) ships the puzzle mobile fixes and the daily gapfill workflow.
