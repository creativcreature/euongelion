# F-171: Brand identity in the tab and in the link

## Metadata

- Feature ID: F-171
- Category: Brand
- Owner: Founder
- Status: shipped

## Scores

- Engineering Score: 0/10
- Founder Score: 0/10
- Baseline Cap Policy: max 6 until founder raises

## User Expectation

- A Euangelion tab is identifiable at a glance in a crowded tab bar.
- A link pasted into a message, a group chat, or a social post shows the page's
  own artwork, not a wall of text and not a blank rectangle.
- The installed PWA carries the same mark as the browser tab.

## 10/10 Definition

- No stock framework assets anywhere in the identity surface.
- Every significant shareable route previews with its own lead artwork.
- A missing or undecodable lead degrades to the typographic card, never to a
  broken or empty preview.

## Methodology References

- M00

## Decision References

- SA-127

## Device Contracts

### Desktop

- 16px tab icon legible; 32/48/64/128/256 present in the .ico for high-DPI.
- OG cards 1200x630, artwork band 348px, type panel beneath.

### Mobile

- 180px apple-touch-icon for iOS home screen.
- 512px maskable icon inset to the safe circle for Android adaptive icons.

## Scope

### In scope

- Replace the stock Next favicon with the cobalt slab-E mark (.ico, .svg,
  apple-icon, PWA rasters).
- Manifest moved off the retired brown palette.
- `image` support on `OgCardProps` plus a picture-led factory.
- JPEG lead derivatives via `npm run build:og-leads`.
- Picture-led cards on `/`, `/series/[slug]`, `/devotional/[slug]`,
  `/seeking-help-georgia`.

### Out of scope

- Redesigning the OG card's typography.
- Changing the browser chrome tint to cobalt (considered, deliberately not done
  — it is a visible change nobody asked for).
- `/sunday` and `/soul-audit` cards, which stay typographic for now.

## Interfaces Touched

- `src/app/favicon.ico`, `src/app/icon.svg`, `src/app/apple-icon.png`
- `public/icons/*`, `public/manifest.json`
- `src/lib/og-card.tsx`
- `src/app/opengraph-image.tsx`, `src/app/series/[slug]/opengraph-image.tsx`,
  `src/app/devotional/[slug]/opengraph-image.tsx`,
  `src/app/seeking-help-georgia/opengraph-image.tsx`
- `scripts/build_og_leads.py`

## Dependencies

- Pillow (already used by other build scripts) for the lead derivatives.

## Acceptance Criteria

- [x] `/favicon.ico` is not the Next default hash.
- [x] Icon SVG contains no `<text>` element.
- [x] Manifest is off `#1a1612`.
- [x] Every series declaring `heroImage` has an OG lead.
- [x] All leads are JPEG.
- [x] Picture-led routes return 200 with artwork; missing lead falls back.

## Test Matrix

- [x] Automated — `__tests__/brand-icons-and-og.test.ts` (7 tests)
- [x] Desktop manual — favicon at 16/32/64 plus tab mock; three OG cards rendered
- [x] Mobile manual — apple-touch and maskable rasters inspected

## Risks and Rollback

- **The `.ico` must be RGBA.** Writing it as RGB makes Next's decoder throw
  `Format error decoding Ico` on every request — caught during this work.
- OG leads are fetched over HTTP at render time via `NEXT_PUBLIC_APP_URL`. If
  that variable is wrong in an environment, cards degrade to text rather than
  failing, which is the intended failure mode.
- Rollback: restore the previous favicon and drop `imagePath` from the four
  routes. `og-card.tsx` stays backward compatible — the image prop is optional.

## Incremental Outcomes Log

| Date       | Commit  | Decision IDs | Feature ID | Change Summary                                                                                             | Verification                                                  | Score Delta | Residual Gaps                           |
| ---------- | ------- | ------------ | ---------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- | ----------- | --------------------------------------- |
| 2026-08-24 | pending | SA-127       | F-170      | Cobalt slab-E mark replaces stock Next favicon; picture-led OG cards on 4 routes; 40 JPEG lead derivatives | type-check, lint, 7 tests, 3 OG routes rendered and inspected | 0           | /sunday and /soul-audit still text-only |

## Next Increment

- [ ] Picture-led cards for `/sunday` and `/soul-audit`.
- [ ] Re-run `build:og-leads` in CI so leads cannot drift from artwork.

---

## The letter roll was adopted (2026-08-27, F-108 r4)

This feature's intro work landed on `main` on 2026-08-24 and was never merged
into the branch production is deployed from, so it never reached readers. Its
two best ideas have now been adopted into F-108 r4:

1. **The per-letter roll** — one `overflow: hidden` mask per letter with two
   stacked copies, the column travelling exactly one face, staggered left to
   right. This is the reference's signature move (telhaclarke.com.au) and the
   founder's "subtle text rollover". Re-expressed there as script-created Web
   Animations rather than a CSS animation, because a CSS animation on a
   `.press-*` element sorts BELOW the script animations and is the exact cascade
   bug that left F-108's hand-off dead for two releases.
2. **The mode's own colours from frame one** — founder 2026-08-24: "If Light
   Mode, Euangelion should be Blue, if Dark Mode it should be white." This
   feature was right that there had only ever been one version; r4 now paints
   the ground in the page's own ground and the word in the masthead's own
   colour, in both themes, from the first painted frame.

**Not adopted: the hand-off flight.** This implementation kept the wordmark
flying to the masthead. On 2026-08-26 the founder rejected exactly that — "you
see the word twice during the animation. Its already there before animations" —
because the masthead is a permanent element of the page and a second centre-
screen copy is redundant. r4 prints in place instead, and an invariant (INV7)
now holds the wordmark inside the masthead's band for the whole sequence.

One correction worth recording: the roll's mask must be `height: 1lh`, not
`1em`. `1em` is the font size; this masthead runs `line-height: 0.9`, so at the
fitted size an em-tall mask stands 22.7px proud of the real line box.
