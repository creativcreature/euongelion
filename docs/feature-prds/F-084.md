# F-084: Legacy Wake-Up Mount Retirement + Settings/Copy Cleanup Pass 2

## Metadata

- Feature ID: F-084
- Category: Platform / Cleanup
- Owner: founder
- Status: shipped (2026-07-27)
- Source: founder pin 2026-07-12 ("place a pin in the wake-up thing — will need to be cleaned up and retired") + fine-tooth-comb mandate 2026-07-27 ("keep going — finish and deploy everything")

## User Expectation

- One canonical surface per thing: `/series` for browsing, `/devotional/[slug]` for reading. Old `/wake-up/*` links keep working via permanent redirects. Settings show only honest, current controls; site copy obeys the no-fear-motivation tone rules.

## What Shipped

- **Wake-Up mount retired**: `src/app/wake-up/**` deleted (landing, series+devotional double-mounts, og-images, RSS feed). Shared readers relocated to their canonical homes (`DevotionalPageClient` → `src/app/devotional/[slug]/`, `SeriesPageClient` → `src/app/series/[slug]/`); the `silo`/`isWake` branching stripped. Permanent redirects in `next.config.ts`: `/wake-up→/series`, `/wake-up/feed.xml→/series`, `/wake-up/series/:slug→/series/:slug`, `/wake-up/devotional/:slug→/devotional/:slug`. Link sweep: footer item removed, tab-bar matcher updated (reader routes light SERIES), sitemap, root error boundary, sunday archive CTA.
- **AI & KEYS repair (SA-026 BYOK preserved)**: the single field labeled "OpenAI key" that silently wrote BOTH the OpenAI and Anthropic keys split into two honest fields; retired Google (upstream model shut down)/MiniMax/NVIDIA picker options and their key fields (router code untouched).
- **Tone-rule copy**: uncited fear stats removed from the identity/peace/hope series contexts (SOURCE-OF-TRUTH open item #11).
- **Orphan cleanup pass 2**: three unwired soul-audit components removed (AuditRerollSection, SavedPathsList, CrisisResourceBanner); stale comments corrected (homepage hero, featured-rail, series count).

## Methodology References

- M00
- docs/PUBLIC-FACING-LANGUAGE.md §4 (tone rules)

## Device Contracts

### Desktop

- Every legacy /wake-up URL 308s to its canonical equivalent; reader and series pages render identically at their canonical routes.

### Mobile

- 375px: tab bar marks SERIES active on /series and /devotional routes; no layout change from the relocation.

## Interfaces Touched

- `next.config.ts` (redirects); `src/app/devotional/[slug]/*`, `src/app/series/[slug]/*` (relocated clients); `src/app/sitemap.ts`, `src/app/error.tsx`, `src/app/sunday/archive/page.tsx`, `src/components/SiteFooter.tsx`, `src/components/MobileTabBar.tsx`; `src/app/settings/page.tsx` (AI card); `src/data/series.ts` (copy); 5 test files updated, 1 removed (wake-up RSS).

## Out of Scope

- Deferred-infra holds (clarifier, phase-5 queue, voice-bank) — kept, documented.
- `/design/imagery-samples` — kept (founder-locked imagery workflow surface).

## Acceptance Criteria

- [x] All four /wake-up redirect rules live; zero non-comment /wake-up references in src (except redirects)
- [x] Readers relocated; silo branching removed; suite green
- [x] AI card: two honest key fields; dead provider options gone
- [x] Fear-stat copy removed from series contexts
- [ ] Founder spot-check: old /wake-up bookmark lands on /series

## Risks and Rollback

- Redirects are additive config; relocation is `git mv` — revert restores the mount. RSS feed retired with the magazine (redirects to /series).

## Incremental Outcomes Log

| Date       | Commit    | Decision IDs | Feature ID | Change Summary                                                                  | Verification                     | Score Delta | Residual Gaps               |
| ---------- | --------- | ------------ | ---------- | ------------------------------------------------------------------------------- | -------------------------------- | ----------- | --------------------------- |
| 2026-07-27 | (pending) | SA-033       | F-084      | Wake-Up mount retired w/ 308s; readers canonicalized; AI card + tone copy fixed | type-check/lint/1762 tests green | 0→shipped   | founder redirect spot-check |

## Next Increment

- [ ] Founder confirms old links redirect correctly in the wild
