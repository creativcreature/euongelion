# F-184: The Daily Bread V2 — a serialized, self-archiving daily paper

## Metadata

- Feature ID: F-184
- Category: Daily Bread
- Owner: founder
- Status: shipped 2026-09-13 — live on euangelion.app (Worker version 44ee9459); `DAILY_BREAD_V2_DEFAULT = 'on'`, rollback `DAILY_BREAD_V2=off`
- Decisions: SA-142
- Surfaces: `/daily-bread`, `/daily-bread/YYYY-MM-DD`, `/daily-bread/archive`,
  `/daily-bread/archive/[date]` (redirects when on), `/admin/preview/daily-bread-v2`,
  `GET /api/admin/daily-bread/health`, `POST /api/admin/daily-bread/publish`,
  `supabase/migrations/20260913000001_daily_bread_v2.sql`, `src/lib/daily-bread/**`,
  `src/components/daily-bread/**`, `design-system/daily-bread-v2*.css`,
  `scripts/daily-bread/cli.mts`, `.github/workflows/daily-bread-v2.yml`.
  Architecture: `docs/daily-bread/DAILY-BREAD-V2.md`. Runbook:
  `docs/runbooks/DAILY-BREAD-V2-RUNBOOK.md`.

## Problem

The founder asked for the Daily Bread to become a real daily publication and for the
comic, which "doesn't load", to be fixed. The SA-090 paper re-computes itself on every
request, derives "Vol. · No." from the calendar (No. resets every 1 January), keeps no
frozen archive (an archived date re-renders with today's code and data), and depends on
a strip pipeline that has produced no rows for weeks. There was no record of how any
day's paper was made and no way to tell a degraded day from a normal one.

## Solution

- **Serialized editions.** One row per editorial date in `daily_bread_editions`.
  Native V2 issues start at Vol. 1 · No. 001; the number is allocated inside the
  publication transaction under an advisory lock and never reused. Backfilled
  editions are archive entries and never take a number.
- **Frozen snapshots.** A published edition renders from its stored document.
  Corrections write immutable revisions (trigger-enforced); withdrawal keeps the
  number retired and shows the reason.
- **Lifecycle ⟂ quality.** draft → assembling → ready → published → superseded,
  independent of normal / fallback / minimum. A fallback edition says so in its
  colophon.
- **Pipeline.** A 7am America/New_York editorial clock; a scheduler step that builds
  tomorrow in the 14 hours before rollover and publishes after it; an assembly lease;
  a PublicationAttempt row for every attempt (providers, fallbacks, stage timings,
  module failures, cost).
- **Generator chain.** Claude (API or Claude Code CLI) → Gemini → a deterministic
  floor. Timeouts, one retry, output validation. The model writes only the editorial
  frame (deck, rabbit-hole reasons, comic storyboard choice, scene choice); Scripture
  text is always looked up in the BSB, never generated.
- **The comic, fixed at the root.** Script and render are split; strips are
  deterministic SVG line art from 17 wordless committed templates (verbatim BSB
  captions) or a validated generated storyboard, with a five-level chain (approved art
  → generated script → template → archive reprint → omitted). Legacy pipeline faults
  repaired: gap-fill installs the Claude CLI; the weekly job's timeout covers its last
  step; tier 3 no longer breaks the Sunday lead; failure alerts can file issues.
- **Composition.** Eight structurally distinct archetypes (Broadsheet, Illuminated,
  Quiet, Field Notes, Red Letter, Study Table, Joy, Prayer Book), seeded choice with
  anti-repeat scoring, module tiers and band rhythm.
- **Procedural visual engine.** First-party WebGL scenes (Living Water, Grain,
  Wilderness Stars) with riso, halftone and ASCII renderers; a static SVG poster is
  always painted first; reduced motion, off-screen pause, 30 fps cap, DPR cap, context
  loss handled. No image model at request time.
- **Security.** Service-role writes only; RLS public read of published rows only;
  pipeline functions revoked from client roles; internal-secret publish endpoint with
  date-window validation and rate limit; safe links and assets; redacted logs;
  document validation rejects credential-shaped strings and script content.

## Platform Contracts

### Desktop

- Six-column band grid; each archetype sets its own measure, band order and
  hierarchy. Previous/next edition navigation and the archive under every paper.
- The procedural scene animates only when visible, motion is allowed and WebGL comes
  up; otherwise the poster stays.

### Mobile

- Every band collapses to one column at 900px; the comic swaps to three stacked panels
  below 700px; the archive list stacks serial, date, title, archetype; no horizontal
  scroll at 390px (verified in the 7-day QA run). Touch targets on edition navigation
  are at least 44px.

## Acceptance

- [x] Migration tested in real Postgres (PGlite): numbering, lease, idempotency, revisions, RLS, grants
- [x] Deterministic, provider and total-AI-outage paths publish a numbered edition
- [x] Eight archetypes render; seven consecutive fixture days use six archetypes
- [x] Comic renders for every template; captions verified verbatim against the BSB corpus
- [x] 7-day desktop + mobile + dark visual QA in a local server (fixture source)
- [x] Flag off: `/daily-bread` renders the SA-090 paper unchanged; V2 date routes 404
- [x] Migration applied to production Supabase; 0 of 9 pipeline functions callable by client roles (anon probe denied)
- [x] Real providers verified: Claude Code CLI (primary), OpenAI `gpt-5-nano` (backup, measured bake-off)
- [x] Workers preview against production data (curl + desktop/mobile screenshots)
- [x] Deployed (founder: "Deploy and commit"); production verified by body content and screenshots
- [ ] Scheduler variable `DAILY_BREAD_V2_SCHEDULER=enabled` and first CI run verified

## Methodology References

- M00
