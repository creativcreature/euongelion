// AUTO-GENERATED by scripts/build-edge-function.mjs — DO NOT EDIT.
// Edit entry.ts and run `npm run build:edge-function`.
var __defProp = Object.defineProperty;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};

// src/lib/bible/books.ts
function alias(name, id) {
  BOOK_ALIASES[name.toLowerCase().replace(/\s+/g, " ").trim()] = id;
}
function lookupBookId(name) {
  if (!name) return null;
  const normalized = name.toLowerCase().replace(/\./g, "").replace(/\s+/g, " ").trim();
  if (normalized in BOOK_ALIASES) return BOOK_ALIASES[normalized];
  const noSpaces = normalized.replace(/\s+/g, "");
  for (const [key, id] of Object.entries(BOOK_ALIASES)) {
    if (key.replace(/\s+/g, "") === noSpaces) return id;
  }
  return null;
}
function isSingleChapterBook(id) {
  return BIBLE_BOOK_META[id].singleChapter;
}
var BIBLE_BOOK_META, BOOK_ALIASES;
var init_books = __esm({
  "src/lib/bible/books.ts"() {
    "use strict";
    BIBLE_BOOK_META = {
      GEN: {
        id: "GEN",
        name: "Genesis",
        testament: "OT",
        chapters: 50,
        singleChapter: false
      },
      EXO: {
        id: "EXO",
        name: "Exodus",
        testament: "OT",
        chapters: 40,
        singleChapter: false
      },
      LEV: {
        id: "LEV",
        name: "Leviticus",
        testament: "OT",
        chapters: 27,
        singleChapter: false
      },
      NUM: {
        id: "NUM",
        name: "Numbers",
        testament: "OT",
        chapters: 36,
        singleChapter: false
      },
      DEU: {
        id: "DEU",
        name: "Deuteronomy",
        testament: "OT",
        chapters: 34,
        singleChapter: false
      },
      JOS: {
        id: "JOS",
        name: "Joshua",
        testament: "OT",
        chapters: 24,
        singleChapter: false
      },
      JDG: {
        id: "JDG",
        name: "Judges",
        testament: "OT",
        chapters: 21,
        singleChapter: false
      },
      RUT: {
        id: "RUT",
        name: "Ruth",
        testament: "OT",
        chapters: 4,
        singleChapter: false
      },
      "1SA": {
        id: "1SA",
        name: "1 Samuel",
        testament: "OT",
        chapters: 31,
        singleChapter: false
      },
      "2SA": {
        id: "2SA",
        name: "2 Samuel",
        testament: "OT",
        chapters: 24,
        singleChapter: false
      },
      "1KI": {
        id: "1KI",
        name: "1 Kings",
        testament: "OT",
        chapters: 22,
        singleChapter: false
      },
      "2KI": {
        id: "2KI",
        name: "2 Kings",
        testament: "OT",
        chapters: 25,
        singleChapter: false
      },
      "1CH": {
        id: "1CH",
        name: "1 Chronicles",
        testament: "OT",
        chapters: 29,
        singleChapter: false
      },
      "2CH": {
        id: "2CH",
        name: "2 Chronicles",
        testament: "OT",
        chapters: 36,
        singleChapter: false
      },
      EZR: {
        id: "EZR",
        name: "Ezra",
        testament: "OT",
        chapters: 10,
        singleChapter: false
      },
      NEH: {
        id: "NEH",
        name: "Nehemiah",
        testament: "OT",
        chapters: 13,
        singleChapter: false
      },
      EST: {
        id: "EST",
        name: "Esther",
        testament: "OT",
        chapters: 10,
        singleChapter: false
      },
      JOB: {
        id: "JOB",
        name: "Job",
        testament: "OT",
        chapters: 42,
        singleChapter: false
      },
      PSA: {
        id: "PSA",
        name: "Psalms",
        testament: "OT",
        chapters: 150,
        singleChapter: false
      },
      PRO: {
        id: "PRO",
        name: "Proverbs",
        testament: "OT",
        chapters: 31,
        singleChapter: false
      },
      ECC: {
        id: "ECC",
        name: "Ecclesiastes",
        testament: "OT",
        chapters: 12,
        singleChapter: false
      },
      SNG: {
        id: "SNG",
        name: "Song of Solomon",
        testament: "OT",
        chapters: 8,
        singleChapter: false
      },
      ISA: {
        id: "ISA",
        name: "Isaiah",
        testament: "OT",
        chapters: 66,
        singleChapter: false
      },
      JER: {
        id: "JER",
        name: "Jeremiah",
        testament: "OT",
        chapters: 52,
        singleChapter: false
      },
      LAM: {
        id: "LAM",
        name: "Lamentations",
        testament: "OT",
        chapters: 5,
        singleChapter: false
      },
      EZK: {
        id: "EZK",
        name: "Ezekiel",
        testament: "OT",
        chapters: 48,
        singleChapter: false
      },
      DAN: {
        id: "DAN",
        name: "Daniel",
        testament: "OT",
        chapters: 12,
        singleChapter: false
      },
      HOS: {
        id: "HOS",
        name: "Hosea",
        testament: "OT",
        chapters: 14,
        singleChapter: false
      },
      JOL: {
        id: "JOL",
        name: "Joel",
        testament: "OT",
        chapters: 3,
        singleChapter: false
      },
      AMO: {
        id: "AMO",
        name: "Amos",
        testament: "OT",
        chapters: 9,
        singleChapter: false
      },
      OBA: {
        id: "OBA",
        name: "Obadiah",
        testament: "OT",
        chapters: 1,
        singleChapter: true
      },
      JON: {
        id: "JON",
        name: "Jonah",
        testament: "OT",
        chapters: 4,
        singleChapter: false
      },
      MIC: {
        id: "MIC",
        name: "Micah",
        testament: "OT",
        chapters: 7,
        singleChapter: false
      },
      NAM: {
        id: "NAM",
        name: "Nahum",
        testament: "OT",
        chapters: 3,
        singleChapter: false
      },
      HAB: {
        id: "HAB",
        name: "Habakkuk",
        testament: "OT",
        chapters: 3,
        singleChapter: false
      },
      ZEP: {
        id: "ZEP",
        name: "Zephaniah",
        testament: "OT",
        chapters: 3,
        singleChapter: false
      },
      HAG: {
        id: "HAG",
        name: "Haggai",
        testament: "OT",
        chapters: 2,
        singleChapter: false
      },
      ZEC: {
        id: "ZEC",
        name: "Zechariah",
        testament: "OT",
        chapters: 14,
        singleChapter: false
      },
      MAL: {
        id: "MAL",
        name: "Malachi",
        testament: "OT",
        chapters: 4,
        singleChapter: false
      },
      MAT: {
        id: "MAT",
        name: "Matthew",
        testament: "NT",
        chapters: 28,
        singleChapter: false
      },
      MRK: {
        id: "MRK",
        name: "Mark",
        testament: "NT",
        chapters: 16,
        singleChapter: false
      },
      LUK: {
        id: "LUK",
        name: "Luke",
        testament: "NT",
        chapters: 24,
        singleChapter: false
      },
      JHN: {
        id: "JHN",
        name: "John",
        testament: "NT",
        chapters: 21,
        singleChapter: false
      },
      ACT: {
        id: "ACT",
        name: "Acts",
        testament: "NT",
        chapters: 28,
        singleChapter: false
      },
      ROM: {
        id: "ROM",
        name: "Romans",
        testament: "NT",
        chapters: 16,
        singleChapter: false
      },
      "1CO": {
        id: "1CO",
        name: "1 Corinthians",
        testament: "NT",
        chapters: 16,
        singleChapter: false
      },
      "2CO": {
        id: "2CO",
        name: "2 Corinthians",
        testament: "NT",
        chapters: 13,
        singleChapter: false
      },
      GAL: {
        id: "GAL",
        name: "Galatians",
        testament: "NT",
        chapters: 6,
        singleChapter: false
      },
      EPH: {
        id: "EPH",
        name: "Ephesians",
        testament: "NT",
        chapters: 6,
        singleChapter: false
      },
      PHP: {
        id: "PHP",
        name: "Philippians",
        testament: "NT",
        chapters: 4,
        singleChapter: false
      },
      COL: {
        id: "COL",
        name: "Colossians",
        testament: "NT",
        chapters: 4,
        singleChapter: false
      },
      "1TH": {
        id: "1TH",
        name: "1 Thessalonians",
        testament: "NT",
        chapters: 5,
        singleChapter: false
      },
      "2TH": {
        id: "2TH",
        name: "2 Thessalonians",
        testament: "NT",
        chapters: 3,
        singleChapter: false
      },
      "1TI": {
        id: "1TI",
        name: "1 Timothy",
        testament: "NT",
        chapters: 6,
        singleChapter: false
      },
      "2TI": {
        id: "2TI",
        name: "2 Timothy",
        testament: "NT",
        chapters: 4,
        singleChapter: false
      },
      TIT: {
        id: "TIT",
        name: "Titus",
        testament: "NT",
        chapters: 3,
        singleChapter: false
      },
      PHM: {
        id: "PHM",
        name: "Philemon",
        testament: "NT",
        chapters: 1,
        singleChapter: true
      },
      HEB: {
        id: "HEB",
        name: "Hebrews",
        testament: "NT",
        chapters: 13,
        singleChapter: false
      },
      JAS: {
        id: "JAS",
        name: "James",
        testament: "NT",
        chapters: 5,
        singleChapter: false
      },
      "1PE": {
        id: "1PE",
        name: "1 Peter",
        testament: "NT",
        chapters: 5,
        singleChapter: false
      },
      "2PE": {
        id: "2PE",
        name: "2 Peter",
        testament: "NT",
        chapters: 3,
        singleChapter: false
      },
      "1JN": {
        id: "1JN",
        name: "1 John",
        testament: "NT",
        chapters: 5,
        singleChapter: false
      },
      "2JN": {
        id: "2JN",
        name: "2 John",
        testament: "NT",
        chapters: 1,
        singleChapter: true
      },
      "3JN": {
        id: "3JN",
        name: "3 John",
        testament: "NT",
        chapters: 1,
        singleChapter: true
      },
      JUD: {
        id: "JUD",
        name: "Jude",
        testament: "NT",
        chapters: 1,
        singleChapter: true
      },
      REV: {
        id: "REV",
        name: "Revelation",
        testament: "NT",
        chapters: 22,
        singleChapter: false
      }
    };
    BOOK_ALIASES = {};
    for (const meta of Object.values(BIBLE_BOOK_META)) {
      alias(meta.name, meta.id);
      alias(meta.id, meta.id);
    }
    alias("Gn", "GEN");
    alias("Gen", "GEN");
    alias("Ge", "GEN");
    alias("Ex", "EXO");
    alias("Exod", "EXO");
    alias("Lv", "LEV");
    alias("Lev", "LEV");
    alias("Nm", "NUM");
    alias("Num", "NUM");
    alias("Nu", "NUM");
    alias("Dt", "DEU");
    alias("Deut", "DEU");
    alias("Deu", "DEU");
    alias("Jos", "JOS");
    alias("Jsh", "JOS");
    alias("Josh", "JOS");
    alias("Jdg", "JDG");
    alias("Judg", "JDG");
    alias("Jg", "JDG");
    alias("Ru", "RUT");
    alias("Rth", "RUT");
    alias("Rut", "RUT");
    alias("1 Sam", "1SA");
    alias("1Sam", "1SA");
    alias("1S", "1SA");
    alias("1Sa", "1SA");
    alias("I Samuel", "1SA");
    alias("First Samuel", "1SA");
    alias("2 Sam", "2SA");
    alias("2Sam", "2SA");
    alias("2S", "2SA");
    alias("2Sa", "2SA");
    alias("II Samuel", "2SA");
    alias("Second Samuel", "2SA");
    alias("1 Kgs", "1KI");
    alias("1Kgs", "1KI");
    alias("1K", "1KI");
    alias("1Ki", "1KI");
    alias("I Kings", "1KI");
    alias("First Kings", "1KI");
    alias("2 Kgs", "2KI");
    alias("2Kgs", "2KI");
    alias("2K", "2KI");
    alias("2Ki", "2KI");
    alias("II Kings", "2KI");
    alias("Second Kings", "2KI");
    alias("1 Chr", "1CH");
    alias("1Chr", "1CH");
    alias("1Ch", "1CH");
    alias("1Chron", "1CH");
    alias("I Chronicles", "1CH");
    alias("First Chronicles", "1CH");
    alias("2 Chr", "2CH");
    alias("2Chr", "2CH");
    alias("2Ch", "2CH");
    alias("2Chron", "2CH");
    alias("II Chronicles", "2CH");
    alias("Second Chronicles", "2CH");
    alias("Ezr", "EZR");
    alias("Neh", "NEH");
    alias("Ne", "NEH");
    alias("Est", "EST");
    alias("Esth", "EST");
    alias("Jb", "JOB");
    alias("Ps", "PSA");
    alias("Psalm", "PSA");
    alias("Pss", "PSA");
    alias("Psa", "PSA");
    alias("Pr", "PRO");
    alias("Prv", "PRO");
    alias("Prov", "PRO");
    alias("Ec", "ECC");
    alias("Eccl", "ECC");
    alias("Qoh", "ECC");
    alias("Qoheleth", "ECC");
    alias("Sg", "SNG");
    alias("Song", "SNG");
    alias("Song of Songs", "SNG");
    alias("Song of Sol", "SNG");
    alias("Canticles", "SNG");
    alias("Cant", "SNG");
    alias("Is", "ISA");
    alias("Isa", "ISA");
    alias("Jer", "JER");
    alias("Jr", "JER");
    alias("Lam", "LAM");
    alias("La", "LAM");
    alias("Ezk", "EZK");
    alias("Ez", "EZK");
    alias("Ezek", "EZK");
    alias("Eze", "EZK");
    alias("Ezekiel", "EZK");
    alias("Dn", "DAN");
    alias("Dan", "DAN");
    alias("Hos", "HOS");
    alias("Ho", "HOS");
    alias("Joel", "JOL");
    alias("Jl", "JOL");
    alias("Joe", "JOL");
    alias("Am", "AMO");
    alias("Amo", "AMO");
    alias("Ob", "OBA");
    alias("Obad", "OBA");
    alias("Jnh", "JON");
    alias("Jon", "JON");
    alias("Mi", "MIC");
    alias("Mic", "MIC");
    alias("Na", "NAM");
    alias("Nah", "NAM");
    alias("Nahum", "NAM");
    alias("Hab", "HAB");
    alias("Hb", "HAB");
    alias("Zep", "ZEP");
    alias("Zeph", "ZEP");
    alias("Zp", "ZEP");
    alias("Hg", "HAG");
    alias("Hag", "HAG");
    alias("Zec", "ZEC");
    alias("Zech", "ZEC");
    alias("Zc", "ZEC");
    alias("Mal", "MAL");
    alias("Ml", "MAL");
    alias("Mt", "MAT");
    alias("Mat", "MAT");
    alias("Matt", "MAT");
    alias("Mk", "MRK");
    alias("Mar", "MRK");
    alias("Mrk", "MRK");
    alias("Lk", "LUK");
    alias("Luk", "LUK");
    alias("Jn", "JHN");
    alias("Jhn", "JHN");
    alias("Joh", "JHN");
    alias("Ac", "ACT");
    alias("Act", "ACT");
    alias("Ro", "ROM");
    alias("Rom", "ROM");
    alias("Rm", "ROM");
    alias("1 Cor", "1CO");
    alias("1Cor", "1CO");
    alias("1Co", "1CO");
    alias("I Corinthians", "1CO");
    alias("First Corinthians", "1CO");
    alias("2 Cor", "2CO");
    alias("2Cor", "2CO");
    alias("2Co", "2CO");
    alias("II Corinthians", "2CO");
    alias("Second Corinthians", "2CO");
    alias("Gal", "GAL");
    alias("Ga", "GAL");
    alias("Eph", "EPH");
    alias("Ephes", "EPH");
    alias("Phil", "PHP");
    alias("Php", "PHP");
    alias("Pp", "PHP");
    alias("Philip", "PHP");
    alias("Col", "COL");
    alias("1 Thess", "1TH");
    alias("1Thess", "1TH");
    alias("1Th", "1TH");
    alias("1Thes", "1TH");
    alias("I Thessalonians", "1TH");
    alias("First Thessalonians", "1TH");
    alias("2 Thess", "2TH");
    alias("2Thess", "2TH");
    alias("2Th", "2TH");
    alias("2Thes", "2TH");
    alias("II Thessalonians", "2TH");
    alias("Second Thessalonians", "2TH");
    alias("1 Tim", "1TI");
    alias("1Tim", "1TI");
    alias("1Ti", "1TI");
    alias("I Timothy", "1TI");
    alias("First Timothy", "1TI");
    alias("2 Tim", "2TI");
    alias("2Tim", "2TI");
    alias("2Ti", "2TI");
    alias("II Timothy", "2TI");
    alias("Second Timothy", "2TI");
    alias("Tit", "TIT");
    alias("Ti", "TIT");
    alias("Phm", "PHM");
    alias("Philem", "PHM");
    alias("Phlm", "PHM");
    alias("Heb", "HEB");
    alias("He", "HEB");
    alias("Jas", "JAS");
    alias("Jm", "JAS");
    alias("Jam", "JAS");
    alias("1 Pet", "1PE");
    alias("1Pet", "1PE");
    alias("1Pe", "1PE");
    alias("1P", "1PE");
    alias("I Peter", "1PE");
    alias("First Peter", "1PE");
    alias("2 Pet", "2PE");
    alias("2Pet", "2PE");
    alias("2Pe", "2PE");
    alias("2P", "2PE");
    alias("II Peter", "2PE");
    alias("Second Peter", "2PE");
    alias("1 Jn", "1JN");
    alias("1Jn", "1JN");
    alias("1Jo", "1JN");
    alias("1J", "1JN");
    alias("1 John", "1JN");
    alias("I John", "1JN");
    alias("First John", "1JN");
    alias("2 Jn", "2JN");
    alias("2Jn", "2JN");
    alias("2Jo", "2JN");
    alias("2J", "2JN");
    alias("2 John", "2JN");
    alias("II John", "2JN");
    alias("Second John", "2JN");
    alias("3 Jn", "3JN");
    alias("3Jn", "3JN");
    alias("3Jo", "3JN");
    alias("3J", "3JN");
    alias("3 John", "3JN");
    alias("III John", "3JN");
    alias("Third John", "3JN");
    alias("Jud", "JUD");
    alias("Jude", "JUD");
    alias("Rv", "REV");
    alias("Rev", "REV");
    alias("Re", "REV");
    alias("Apoc", "REV");
    alias("Revelation of John", "REV");
    alias("Revelation of Jesus Christ", "REV");
    alias("The Revelation", "REV");
    alias("Song of Songs", "SNG");
    alias("Psalm", "PSA");
  }
});

// src/lib/bible/parseReference.ts
function normalizeWhitespaceAndDashes(value) {
  return value.replace(new RegExp(RANGE_DASH.source, "g"), "-").replace(/\s+/g, " ").trim();
}
function buildCanonical(parts) {
  const {
    bookName,
    startChapter,
    startVerse,
    endChapter,
    endVerse,
    isSingleChapter
  } = parts;
  if (isSingleChapter) {
    if (startVerse === endVerse) return `${bookName} ${startVerse}`;
    return `${bookName} ${startVerse}-${endVerse}`;
  }
  if (startVerse === 0 && endVerse === 0 && startChapter === endChapter) {
    return `${bookName} ${startChapter}`;
  }
  if (startChapter === endChapter) {
    if (startVerse === endVerse)
      return `${bookName} ${startChapter}:${startVerse}`;
    return `${bookName} ${startChapter}:${startVerse}-${endVerse}`;
  }
  return `${bookName} ${startChapter}:${startVerse}-${endChapter}:${endVerse}`;
}
function parseReference(reference) {
  if (!reference || typeof reference !== "string") return null;
  const normalized = normalizeWhitespaceAndDashes(reference);
  if (!normalized) return null;
  const match = normalized.match(/^(.+?)\s+(\d.*)$/);
  if (!match) return null;
  const rawBook = match[1].trim();
  const rest = match[2].trim();
  const bookId = lookupBookId(rawBook);
  if (!bookId) return null;
  const meta = BIBLE_BOOK_META[bookId];
  const single = isSingleChapterBook(bookId);
  const numericPattern = /^(\d+)(?::(\d+))?(?:-(\d+)(?::(\d+))?)?$/;
  const numbers = rest.match(numericPattern);
  if (!numbers) return null;
  const a = parseInt(numbers[1], 10);
  const b = numbers[2] ? parseInt(numbers[2], 10) : null;
  const c = numbers[3] ? parseInt(numbers[3], 10) : null;
  const d = numbers[4] ? parseInt(numbers[4], 10) : null;
  let startChapter;
  let startVerse;
  let endChapter;
  let endVerse;
  if (single) {
    if (b !== null) return null;
    startChapter = 1;
    endChapter = 1;
    startVerse = a;
    endVerse = c !== null ? c : a;
  } else if (b === null) {
    startChapter = a;
    endChapter = c !== null ? c : a;
    startVerse = 0;
    endVerse = 0;
  } else if (c === null) {
    startChapter = a;
    endChapter = a;
    startVerse = b;
    endVerse = b;
  } else if (d === null) {
    startChapter = a;
    endChapter = a;
    startVerse = b;
    endVerse = c;
  } else {
    startChapter = a;
    startVerse = b;
    endChapter = c;
    endVerse = d;
  }
  if (startChapter < 1 || endChapter < 1) return null;
  if (startChapter > meta.chapters || endChapter > meta.chapters) return null;
  if (startVerse < 0 || endVerse < 0) return null;
  if (endChapter < startChapter || endChapter === startChapter && endVerse !== 0 && endVerse < startVerse) {
    return null;
  }
  return {
    book: bookId,
    bookName: meta.name,
    startChapter,
    startVerse,
    endChapter,
    endVerse,
    canonical: buildCanonical({
      bookName: meta.name,
      startChapter,
      startVerse,
      endChapter,
      endVerse,
      isSingleChapter: single
    })
  };
}
var RANGE_DASH;
var init_parseReference = __esm({
  "src/lib/bible/parseReference.ts"() {
    "use strict";
    init_books();
    RANGE_DASH = /[‐-―−-]/;
  }
});

// src/lib/bible/translations.ts
var DEFAULT_BIBLE_TRANSLATION, BIBLE_TRANSLATIONS;
var init_translations = __esm({
  "src/lib/bible/translations.ts"() {
    "use strict";
    DEFAULT_BIBLE_TRANSLATION = "BSB";
    BIBLE_TRANSLATIONS = {
      BSB: {
        code: "BSB",
        name: "Berean Standard Bible",
        short: "BSB",
        year: 2022,
        license: "Public domain (CC0 1.0 Universal)",
        licenseShort: "CC0",
        source: "bereanbible.com",
        tone: "Modern, clear, readable",
        description: "The Berean Standard Bible is a modern English translation released to the public domain via Creative Commons Zero. Default for clarity and readability."
      },
      WEB: {
        code: "WEB",
        name: "World English Bible",
        short: "WEB",
        year: 2e3,
        license: "Public domain",
        licenseShort: "PD",
        source: "ebible.org",
        tone: "Modern, accessible",
        description: "A modern revision of the American Standard Version, released to the public domain by Michael Johnson and ebible.org."
      },
      KJV: {
        code: "KJV",
        name: "King James Version",
        short: "KJV",
        year: 1769,
        license: "Public domain in the United States",
        licenseShort: "PD (US)",
        source: "ebible.org",
        tone: "Literary, traditional",
        description: "The 1769 Cambridge standardized text of the King James Version. Public domain in the United States; Crown Copyright in the United Kingdom by way of Letters Patent."
      },
      ASV: {
        code: "ASV",
        name: "American Standard Version",
        short: "ASV",
        year: 1901,
        license: "Public domain",
        licenseShort: "PD",
        source: "ebible.org",
        tone: "Literal, formal",
        description: "Word-for-word literal translation from 1901. Useful alongside Hebrew and Greek vocabulary modules."
      },
      YLT: {
        code: "YLT",
        name: "Young's Literal Translation",
        short: "YLT",
        year: 1898,
        license: "Public domain",
        licenseShort: "PD",
        source: "ebible.org",
        tone: "Hyper-literal",
        description: "Robert Young's 1898 hyper-literal translation. Surfaces verb tenses and grammatical structure of the original languages."
      },
      DARBY: {
        code: "DARBY",
        name: "Darby Translation",
        short: "DARBY",
        year: 1890,
        license: "Public domain",
        licenseShort: "PD",
        source: "ebible.org",
        tone: "Precise, technical",
        description: "John Nelson Darby\u2019s 1890 translation. Known for precise translation choices, especially in the Pauline epistles."
      },
      BBE: {
        code: "BBE",
        name: "Bible in Basic English",
        short: "BBE",
        year: 1949,
        license: "Public domain in the United States",
        licenseShort: "PD (US)",
        source: "ebible.org",
        tone: "Simplest English",
        description: "Bible in Basic English (1949), produced under the direction of S. H. Hooke at the University of London. Lowest-literacy entry point."
      }
    };
  }
});

// node_modules/@opennextjs/cloudflare/dist/api/cloudflare-context.js
function getCloudflareContext(options = { async: false }) {
  return options.async ? getCloudflareContextAsync() : getCloudflareContextSync();
}
function getCloudflareContextFromGlobalScope() {
  const global = globalThis;
  return global[cloudflareContextSymbol];
}
function inSSG() {
  const global = globalThis;
  return global.__NEXT_DATA__?.nextExport === true;
}
function getCloudflareContextSync() {
  const cloudflareContext = getCloudflareContextFromGlobalScope();
  if (cloudflareContext) {
    return cloudflareContext;
  }
  if (inSSG()) {
    throw new Error(`

ERROR: \`getCloudflareContext\` has been called in sync mode in either a static route or at the top level of a non-static one, both cases are not allowed but can be solved by either:
  - make sure that the call is not at the top level and that the route is not static
  - call \`getCloudflareContext({async: true})\` to use the \`async\` mode
  - avoid calling \`getCloudflareContext\` in the route
`);
  }
  throw new Error(initOpenNextCloudflareForDevErrorMsg);
}
async function getCloudflareContextAsync() {
  const cloudflareContext = getCloudflareContextFromGlobalScope();
  if (cloudflareContext) {
    return cloudflareContext;
  }
  const inNodejsRuntime = process.env.NEXT_RUNTIME === "nodejs";
  if (inNodejsRuntime || inSSG()) {
    const cloudflareContext2 = await getCloudflareContextFromWrangler();
    addCloudflareContextToNodejsGlobal(cloudflareContext2);
    return cloudflareContext2;
  }
  throw new Error(initOpenNextCloudflareForDevErrorMsg);
}
async function initOpenNextCloudflareForDev(options) {
  const shouldInitializationRun = shouldContextInitializationRun();
  if (!shouldInitializationRun)
    return;
  if (options?.environment && process.env.NEXT_DEV_WRANGLER_ENV) {
    console.warn(`'initOpenNextCloudflareForDev' has been called with an environment option while NEXT_DEV_WRANGLER_ENV is set. NEXT_DEV_WRANGLER_ENV will be ignored and the environment will be set to: '${options.environment}'`);
  }
  const context = await getCloudflareContextFromWrangler(options);
  addCloudflareContextToNodejsGlobal(context);
  await monkeyPatchVmModuleEdgeContext(context);
}
function shouldContextInitializationRun() {
  const AsyncLocalStorage = globalThis["AsyncLocalStorage"];
  return !!AsyncLocalStorage;
}
function addCloudflareContextToNodejsGlobal(cloudflareContext) {
  const global = globalThis;
  global[cloudflareContextSymbol] = cloudflareContext;
}
async function monkeyPatchVmModuleEdgeContext(cloudflareContext) {
  const require2 = (await import(
    /* webpackIgnore: true */
    `${"__module".replaceAll("_", "")}`
  )).default.createRequire(import.meta.url);
  const vmModule = require2("vm");
  const originalRunInContext = vmModule.runInContext.bind(vmModule);
  vmModule.runInContext = (code, contextifiedObject, options) => {
    const runtimeContext = contextifiedObject;
    runtimeContext[cloudflareContextSymbol] ??= cloudflareContext;
    return originalRunInContext(code, contextifiedObject, options);
  };
}
async function getCloudflareContextFromWrangler(options) {
  const { getPlatformProxy } = await import(
    /* webpackIgnore: true */
    `${"__wrangler".replaceAll("_", "")}`
  );
  const environment = options?.environment ?? process.env.NEXT_DEV_WRANGLER_ENV;
  const { env, cf, ctx } = await getPlatformProxy({
    ...options,
    // The `env` passed to the fetch handler does not contain variables from `.env*` files.
    // because we invoke wrangler with `CLOUDFLARE_LOAD_DEV_VARS_FROM_DOT_ENV`=`"false"`.
    // Initializing `envFiles` with an empty list is the equivalent for this API call.
    envFiles: [],
    environment
  });
  return {
    env,
    cf,
    ctx
  };
}
var cloudflareContextSymbol, initOpenNextCloudflareForDevErrorMsg;
var init_cloudflare_context = __esm({
  "node_modules/@opennextjs/cloudflare/dist/api/cloudflare-context.js"() {
    cloudflareContextSymbol = /* @__PURE__ */ Symbol.for("__cloudflare-context__");
    initOpenNextCloudflareForDevErrorMsg = `

ERROR: \`getCloudflareContext\` has been called without having called \`initOpenNextCloudflareForDev\` from the Next.js config file.
You should update your Next.js config file as shown below:

   \`\`\`
   // next.config.mjs

   import { initOpenNextCloudflareForDev } from "@opennextjs/cloudflare";

   initOpenNextCloudflareForDev();

   const nextConfig = { ... };
   export default nextConfig;
   \`\`\`

`;
  }
});

// node_modules/@opennextjs/cloudflare/dist/api/overrides/asset-resolver/index.js
function getResponseBody(method, response) {
  if (method === "HEAD") {
    return null;
  }
  return response.body || new ReadableStream();
}
function isUserWorkerFirst(runWorkerFirst, pathname) {
  if (!Array.isArray(runWorkerFirst)) {
    return runWorkerFirst ?? false;
  }
  let hasPositiveMatch = false;
  for (let rule of runWorkerFirst) {
    let isPositiveRule = true;
    if (rule.startsWith("!")) {
      rule = rule.slice(1);
      isPositiveRule = false;
    } else if (hasPositiveMatch) {
      continue;
    }
    const match = new RegExp(`^${rule.replace(/([[\]().*+?^$|{}\\])/g, "\\$1").replace("\\*", ".*")}$`).test(pathname);
    if (match) {
      if (isPositiveRule) {
        hasPositiveMatch = true;
      } else {
        return false;
      }
    }
  }
  return hasPositiveMatch;
}
var resolver, asset_resolver_default;
var init_asset_resolver = __esm({
  "node_modules/@opennextjs/cloudflare/dist/api/overrides/asset-resolver/index.js"() {
    init_cloudflare_context();
    resolver = {
      name: "cloudflare-asset-resolver",
      async maybeGetAssetResult(event) {
        const { ASSETS } = getCloudflareContext().env;
        if (!ASSETS || !isUserWorkerFirst(globalThis.__ASSETS_RUN_WORKER_FIRST__, event.rawPath)) {
          return void 0;
        }
        const { method, headers } = event;
        if (method !== "GET" && method != "HEAD") {
          return void 0;
        }
        const url = new URL(event.rawPath, "https://assets.local");
        const response = await ASSETS.fetch(url, {
          headers,
          method
        });
        if (response.status === 404) {
          await response.body?.cancel();
          return void 0;
        }
        return {
          type: "core",
          statusCode: response.status,
          headers: Object.fromEntries(response.headers.entries()),
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          body: getResponseBody(method, response),
          isBase64Encoded: false
        };
      }
    };
    asset_resolver_default = resolver;
  }
});

// node_modules/@opennextjs/cloudflare/dist/api/config.js
function defineCloudflareConfig(config = {}) {
  const { incrementalCache, tagCache, queue, cachePurge, enableCacheInterception = false, routePreloadingBehavior = "none" } = config;
  return {
    default: {
      override: {
        wrapper: "cloudflare-node",
        converter: "edge",
        proxyExternalRequest: "fetch",
        incrementalCache: resolveIncrementalCache(incrementalCache),
        tagCache: resolveTagCache(tagCache),
        queue: resolveQueue(queue),
        cdnInvalidation: resolveCdnInvalidation(cachePurge)
      },
      routePreloadingBehavior
    },
    // node:crypto is used to compute cache keys
    edgeExternals: ["node:crypto"],
    cloudflare: {
      useWorkerdCondition: true
    },
    dangerous: {
      enableCacheInterception
    },
    middleware: {
      external: true,
      override: {
        wrapper: "cloudflare-edge",
        converter: "edge",
        proxyExternalRequest: "fetch",
        incrementalCache: resolveIncrementalCache(incrementalCache),
        tagCache: resolveTagCache(tagCache),
        queue: resolveQueue(queue)
      },
      assetResolver: () => asset_resolver_default
    }
  };
}
function resolveIncrementalCache(value = "dummy") {
  if (typeof value === "string") {
    return value;
  }
  return typeof value === "function" ? value : () => value;
}
function resolveTagCache(value = "dummy") {
  if (typeof value === "string") {
    return value;
  }
  return typeof value === "function" ? value : () => value;
}
function resolveQueue(value = "dummy") {
  if (typeof value === "string") {
    return value;
  }
  return typeof value === "function" ? value : () => value;
}
function resolveCdnInvalidation(value = "dummy") {
  if (typeof value === "string") {
    return value;
  }
  return typeof value === "function" ? value : () => value;
}
function getDeploymentId() {
  return `dpl-${(/* @__PURE__ */ new Date()).getTime().toString(36)}`;
}
var init_config = __esm({
  "node_modules/@opennextjs/cloudflare/dist/api/config.js"() {
    init_asset_resolver();
  }
});

// node_modules/@opennextjs/cloudflare/dist/api/index.js
var api_exports = {};
__export(api_exports, {
  defineCloudflareConfig: () => defineCloudflareConfig,
  getCloudflareContext: () => getCloudflareContext,
  getDeploymentId: () => getDeploymentId,
  initOpenNextCloudflareForDev: () => initOpenNextCloudflareForDev
});
var init_api = __esm({
  "node_modules/@opennextjs/cloudflare/dist/api/index.js"() {
    init_cloudflare_context();
    init_config();
  }
});

// src/lib/bible/getVerse.ts
import { promises as fs } from "node:fs";
import path from "node:path";
function bookCacheKey(translation, book) {
  return `${translation}/${book}`;
}
async function loadBook(translation, book) {
  const cacheKey = bookCacheKey(translation, book);
  const cached = bookCache.get(cacheKey);
  if (cached) return cached;
  const assetPath = `bibles/${translation}/${book}.json`;
  let raw = null;
  try {
    const { getCloudflareContext: getCloudflareContext2 } = await Promise.resolve().then(() => (init_api(), api_exports));
    const { env } = await getCloudflareContext2({ async: true });
    const assets = env?.ASSETS;
    if (assets) {
      const res = await assets.fetch(`https://assets.local/${assetPath}`);
      if (res.ok) raw = await res.text();
    }
  } catch {
  }
  if (raw === null) {
    try {
      const filePath = path.join(process.cwd(), "public", assetPath);
      raw = await fs.readFile(filePath, "utf8");
    } catch {
    }
  }
  if (raw === null) {
    try {
      const baseUrl = process.env.NEXT_PUBLIC_APP_URL || "https://euangelion.app";
      const res = await fetch(`${baseUrl}/${assetPath}`);
      if (res.ok) raw = await res.text();
    } catch {
    }
  }
  if (raw === null) {
    throw new Error(
      `Bible corpus file missing: ${translation}/${book}.json (tried ASSETS, fs, self-fetch)`
    );
  }
  let parsed;
  try {
    parsed = JSON.parse(raw);
  } catch (err) {
    throw new Error(
      `Bible corpus file is not valid JSON: ${translation}/${book}.json (${err.message})`
    );
  }
  bookCache.set(cacheKey, parsed);
  return parsed;
}
function collectVerses(data, parsed) {
  const out = [];
  if (parsed.startVerse === 0 && parsed.endVerse === 0) {
    for (let c = parsed.startChapter; c <= parsed.endChapter; c++) {
      const chapter = data[String(c)];
      if (!chapter) continue;
      for (const verseKey of Object.keys(chapter).sort(
        (a, b) => Number(a) - Number(b)
      )) {
        out.push({
          chapter: c,
          verse: Number(verseKey),
          text: chapter[verseKey]
        });
      }
    }
    return out;
  }
  if (parsed.startChapter === parsed.endChapter) {
    const chapter = data[String(parsed.startChapter)];
    if (!chapter) return out;
    for (let v = parsed.startVerse; v <= parsed.endVerse; v++) {
      const text = chapter[String(v)];
      if (text !== void 0) {
        out.push({ chapter: parsed.startChapter, verse: v, text });
      }
    }
    return out;
  }
  for (let c = parsed.startChapter; c <= parsed.endChapter; c++) {
    const chapter = data[String(c)];
    if (!chapter) continue;
    const sortedVerses = Object.keys(chapter).map(Number).sort((a, b) => a - b);
    for (const v of sortedVerses) {
      if (c === parsed.startChapter && v < parsed.startVerse) continue;
      if (c === parsed.endChapter && v > parsed.endVerse) continue;
      out.push({ chapter: c, verse: v, text: chapter[String(v)] });
    }
  }
  return out;
}
function joinVerseText(verses) {
  return verses.map((v) => v.text).join(" ");
}
async function getVerse(reference, translation) {
  if (!BIBLE_TRANSLATIONS[translation]) {
    throw new Error(`Unknown Bible translation: ${translation}`);
  }
  const parsed = parseReference(reference);
  if (!parsed) {
    throw new Error(`Could not parse Scripture reference: "${reference}"`);
  }
  const data = await loadBook(translation, parsed.book);
  const verses = collectVerses(data, parsed);
  if (verses.length === 0) {
    throw new Error(
      `No verses found for "${parsed.canonical}" in ${translation}. Book: ${BIBLE_BOOK_META[parsed.book].name}.`
    );
  }
  return {
    reference,
    canonical: parsed.canonical,
    translation,
    verses,
    text: joinVerseText(verses)
  };
}
var bookCache;
var init_getVerse = __esm({
  "src/lib/bible/getVerse.ts"() {
    "use strict";
    init_parseReference();
    init_books();
    init_translations();
    bookCache = /* @__PURE__ */ new Map();
  }
});

// src/lib/soul-audit/reference-index-loader.ts
import fs2 from "node:fs";
import path2 from "node:path";
async function loadReferenceIndex() {
  if (cachedIndex) return cachedIndex;
  if (loadPromise) return loadPromise;
  loadPromise = (async () => {
    try {
      const { getCloudflareContext: getCloudflareContext2 } = await Promise.resolve().then(() => (init_api(), api_exports));
      const { env } = await getCloudflareContext2({ async: true });
      if (env?.ASSETS) {
        const res = await env.ASSETS.fetch(
          new Request("https://assets.local/reference-index.json")
        );
        if (res.ok) {
          const data = await res.json();
          if (Array.isArray(data) && data.length > 0) {
            cachedIndex = data;
            return cachedIndex;
          }
        }
      }
    } catch {
    }
    try {
      const indexPath = path2.join(
        process.cwd(),
        "public",
        "reference-index.json"
      );
      if (fs2.existsSync(indexPath)) {
        const raw = fs2.readFileSync(indexPath, "utf8");
        const data = JSON.parse(raw);
        if (Array.isArray(data) && data.length > 0) {
          cachedIndex = data;
          return cachedIndex;
        }
      }
    } catch {
    }
    try {
      const baseUrl = process.env.NEXT_PUBLIC_APP_URL || "https://euangelion.app";
      const res = await fetch(`${baseUrl}/reference-index.json`);
      if (res.ok) {
        const data = await res.json();
        if (Array.isArray(data) && data.length > 0) {
          cachedIndex = data;
          return cachedIndex;
        }
      }
    } catch {
    }
    throw new Error("Reference library failed to load from all strategies");
  })();
  try {
    return await loadPromise;
  } finally {
    loadPromise = null;
  }
}
var cachedIndex, loadPromise;
var init_reference_index_loader = __esm({
  "src/lib/soul-audit/reference-index-loader.ts"() {
    "use strict";
    cachedIndex = null;
    loadPromise = null;
  }
});

// src/lib/soul-audit/chunk-retrieval.ts
function bm25Score(query, doc, k1 = 1.5, b = 0.75, avgDl = 500) {
  const queryTerms = query.toLowerCase().split(/\s+/).filter((t) => t.length > 2);
  const docTerms = doc.toLowerCase().split(/\s+/);
  const dl = docTerms.length;
  const tf = /* @__PURE__ */ new Map();
  for (const t of docTerms) tf.set(t, (tf.get(t) || 0) + 1);
  let score = 0;
  for (const qt of queryTerms) {
    const f = tf.get(qt) || 0;
    if (f === 0) continue;
    score += f * (k1 + 1) / (f + k1 * (1 - b + b * dl / avgDl));
  }
  return score;
}
function applyDiversityPenalty(scores, usedChunkIds) {
  const penalized = new Map(scores);
  for (const [id, score] of penalized) {
    if (usedChunkIds.includes(id)) penalized.set(id, score * 0.3);
  }
  return penalized;
}
function selectTopChunks(chunks, query, usedChunkIds, topK, minScore, minRequired) {
  const rawScores = /* @__PURE__ */ new Map();
  for (const chunk of chunks) {
    const text = [
      chunk.title,
      chunk.content,
      chunk.contextualSummary ?? "",
      chunk.keywords.join(" "),
      chunk.scriptureRefs.join(" ")
    ].join(" ");
    rawScores.set(chunk.id, bm25Score(query, text));
  }
  const penalizedScores = applyDiversityPenalty(rawScores, usedChunkIds);
  const sorted = [...penalizedScores.entries()].sort((a, b) => b[1] - a[1]).filter(([, score]) => score > minScore).slice(0, topK);
  if (sorted.length < minRequired) {
    const relaxed = [...penalizedScores.entries()].sort((a, b) => b[1] - a[1]).slice(0, minRequired);
    const relaxedIds = relaxed.map(([id]) => id);
    const relaxedChunks = relaxedIds.map((id) => chunks.find((c) => c.id === id)).filter((c) => c !== void 0);
    return { selected: relaxedChunks, selectedIds: relaxedIds };
  }
  const selectedIds = sorted.map(([id]) => id);
  const selected = selectedIds.map((id) => chunks.find((c) => c.id === id)).filter((c) => c !== void 0);
  return { selected, selectedIds };
}
function attributionFromChunk(chunk) {
  const parts = chunk.source.split("/").filter(Boolean);
  const file = parts[parts.length - 1]?.replace(/\.[a-z]+$/i, "") ?? "";
  const author = parts[parts.length - 2] ?? "";
  const titleize = (s) => s.replace(/[-_]+/g, " ").replace(/\b\w/g, (c) => c.toUpperCase()).replace(/\bA\b/g, "\xE0").trim();
  const authorName = titleize(author);
  const workName = titleize(file);
  return workName ? `${authorName}, ${workName}` : authorName;
}
var init_chunk_retrieval = __esm({
  "src/lib/soul-audit/chunk-retrieval.ts"() {
    "use strict";
  }
});

// src/lib/soul-audit/lexicon.ts
import { promises as fs3 } from "node:fs";
import fsSync from "node:fs";
import path3 from "node:path";
function tupleToEntry(t, pool) {
  const entry = {
    s: pool[t[0]] ?? "",
    w: pool[t[1]] ?? ""
  };
  const eIdx = t[2];
  if (typeof eIdx === "number" && eIdx !== -1) {
    const eStr = pool[eIdx];
    if (eStr) entry.e = eStr;
  }
  const x = t[3];
  if (typeof x === "string" && x) entry.x = x;
  return entry;
}
async function loadPrecomputedAsset(assetName) {
  try {
    const { getCloudflareContext: getCloudflareContext2 } = await Promise.resolve().then(() => (init_api(), api_exports));
    const { env } = await getCloudflareContext2({ async: true });
    if (env?.ASSETS) {
      const res = await env.ASSETS.fetch(
        new Request(`https://assets.local/${assetName}`)
      );
      if (res.ok) return await res.json();
    }
  } catch {
  }
  try {
    const filePath = path3.join(process.cwd(), "public", assetName);
    if (fsSync.existsSync(filePath)) {
      const raw = await fs3.readFile(filePath, "utf8");
      return JSON.parse(raw);
    }
  } catch (err) {
    throw new Error(
      `Precomputed lexicon asset is unreadable: ${assetName} (${err.message})`
    );
  }
  try {
    const baseUrl = process.env.NEXT_PUBLIC_APP_URL || "https://euangelion.app";
    const res = await fetch(`${baseUrl}/${assetName}`);
    if (res.ok) return await res.json();
  } catch {
  }
  return null;
}
async function loadStrongsIndex() {
  if (strongsIndex) return strongsIndex;
  if (!strongsIndexPromise) {
    strongsIndexPromise = loadPrecomputedAsset(
      STRONGS_INDEX_ASSET
    ).then((data) => {
      if (data) strongsIndex = data;
      return data;
    });
  }
  return strongsIndexPromise;
}
async function loadVersesIndex() {
  if (versesIndex) return versesIndex;
  if (!versesIndexPromise) {
    versesIndexPromise = loadPrecomputedAsset(
      VERSES_INDEX_ASSET
    ).then((data) => {
      if (data) versesIndex = data;
      return data;
    });
  }
  return versesIndexPromise;
}
function tagntFileForBook(osis) {
  const which = GREEK_TAGNT_MAT_JHN.has(osis) ? "TAGNT Mat-Jhn - Translators Amalgamated Greek NT - STEPBible.org CC-BY.txt" : "TAGNT Act-Rev - Translators Amalgamated Greek NT - STEPBible.org CC-BY.txt";
  return path3.join(STEPBIBLE_ROOT, which);
}
function decodeEntities(value) {
  return value.replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&quot;/g, '"').replace(/&apos;/g, "'").replace(/&#(\d+);/g, (_, n) => String.fromCodePoint(Number(n))).replace(
    /&#x([0-9a-fA-F]+);/g,
    (_, n) => String.fromCodePoint(parseInt(n, 16))
  ).replace(/&amp;/g, "&");
}
function stripTags(value) {
  return value.replace(/<[^>]*>/g, "");
}
function collapseWhitespace(value) {
  return value.replace(/\s+/g, " ").trim();
}
function cleanText(value) {
  return collapseWhitespace(decodeEntities(stripTags(value)));
}
function limitGloss(value) {
  const cleaned = collapseWhitespace(value);
  if (cleaned.length <= 400) return cleaned;
  const slice = cleaned.slice(0, 400);
  const lastStop = Math.max(
    slice.lastIndexOf(". "),
    slice.lastIndexOf("; "),
    slice.lastIndexOf(", ")
  );
  return (lastStop > 120 ? slice.slice(0, lastStop) : slice).trim() + "\u2026";
}
function hebrewStrongFromLemma(lemma) {
  const segments = lemma.split("/");
  const last = segments[segments.length - 1]?.trim();
  if (!last) return null;
  const m = last.match(/^(\d+)/);
  if (!m) return null;
  return `H${parseInt(m[1], 10)}`;
}
function isHebrewContentWord(morph, strong) {
  if (strong === "H853") return false;
  const body = morph.startsWith("H") ? morph.slice(1) : morph;
  const segs = body.split("/");
  const host = segs[segs.length - 1] ?? "";
  const posLetter = host.charAt(0);
  if (!HEBREW_CONTENT_POS.has(posLetter)) return false;
  if (host.startsWith("Np")) return false;
  return true;
}
function isGreekContentMorph(morph) {
  const head = morph.split("-")[0];
  return head === "N" || head === "V" || head === "A";
}
async function loadHebrewStrong() {
  if (hebrewStrongIndex) return hebrewStrongIndex;
  if (!hebrewStrongPromise) {
    hebrewStrongPromise = (async () => {
      const file = path3.join(
        REFERENCE_ROOT,
        "HebrewLexicon",
        "HebrewStrong.xml"
      );
      const raw = await fs3.readFile(file, "utf8");
      const map = /* @__PURE__ */ new Map();
      const entryRe = /<entry id="(H\d+)">([\s\S]*?)<\/entry>/g;
      let m;
      while ((m = entryRe.exec(raw)) !== null) {
        const id = m[1];
        const body = m[2];
        const wMatch = body.match(
          /<w\b[^>]*\bxlit="([^"]*)"[^>]*>([\s\S]*?)<\/w>/
        );
        const word = wMatch ? cleanText(wMatch[2]) : "";
        const xlit = wMatch ? decodeEntities(wMatch[1]) : "";
        const meaningMatch = body.match(/<meaning>([\s\S]*?)<\/meaning>/);
        const meaning = meaningMatch ? cleanText(meaningMatch[1]) : "";
        map.set(id, { word, xlit, meaning });
      }
      hebrewStrongIndex = map;
      return map;
    })();
  }
  return hebrewStrongPromise;
}
async function loadBdbXref() {
  if (bdbXrefIndex) return bdbXrefIndex;
  if (!bdbXrefPromise) {
    bdbXrefPromise = (async () => {
      const file = path3.join(
        REFERENCE_ROOT,
        "HebrewLexicon",
        "LexicalIndex.xml"
      );
      const raw = await fs3.readFile(file, "utf8");
      const map = /* @__PURE__ */ new Map();
      const isPrimary = /* @__PURE__ */ new Set();
      const xrefRe = /<xref\b([^>]*)\/>/g;
      let m;
      while ((m = xrefRe.exec(raw)) !== null) {
        const attrs = m[1];
        const strongM = attrs.match(/\bstrong="(\d+)"/);
        const bdbM = attrs.match(/\bbdb="([^"]+)"/);
        if (!strongM || !bdbM) continue;
        const key = strongM[1];
        const augM = attrs.match(/\baug="([a-z])"/);
        const primary = !augM || augM[1] === "a";
        if (!map.has(key)) {
          map.set(key, bdbM[1]);
          if (primary) isPrimary.add(key);
        } else if (primary && !isPrimary.has(key)) {
          map.set(key, bdbM[1]);
          isPrimary.add(key);
        }
      }
      bdbXrefIndex = map;
      return map;
    })();
  }
  return bdbXrefPromise;
}
async function loadBdbEntries() {
  if (bdbEntryIndex) return bdbEntryIndex;
  if (!bdbEntryPromise) {
    bdbEntryPromise = (async () => {
      const file = path3.join(
        REFERENCE_ROOT,
        "HebrewLexicon",
        "BrownDriverBriggs.xml"
      );
      const raw = await fs3.readFile(file, "utf8");
      const map = /* @__PURE__ */ new Map();
      const entryRe = /<entry id="([^"]+)"[^>]*>([\s\S]*?)<\/entry>/g;
      let m;
      while ((m = entryRe.exec(raw)) !== null) {
        const id = m[1];
        const body = m[2];
        const defs = [];
        const defRe = /<def>([\s\S]*?)<\/def>/g;
        let d;
        while ((d = defRe.exec(body)) !== null) {
          const text = cleanText(d[1]);
          if (text && !defs.includes(text)) defs.push(text);
          if (defs.length >= 8) break;
        }
        if (defs.length > 0) map.set(id, defs.join("; "));
      }
      bdbEntryIndex = map;
      return map;
    })();
  }
  return bdbEntryPromise;
}
async function loadAbbottSmith() {
  if (abbottIndex) return abbottIndex;
  if (!abbottPromise) {
    abbottPromise = (async () => {
      const file = path3.join(
        REFERENCE_ROOT,
        "Abbott-Smith",
        "abbott-smith.tei.xml"
      );
      const raw = await fs3.readFile(file, "utf8");
      const map = /* @__PURE__ */ new Map();
      const entryRe = /<entry n="([^"]*\|G\d+)">([\s\S]*?)<\/entry>/g;
      let m;
      while ((m = entryRe.exec(raw)) !== null) {
        const nAttr = decodeEntities(m[1]);
        const body = m[2];
        const pipe = nAttr.lastIndexOf("|");
        if (pipe < 0) continue;
        const word = nAttr.slice(0, pipe).trim();
        const id = nAttr.slice(pipe + 1).trim();
        const glosses = [];
        const glossRe = /<gloss>([\s\S]*?)<\/gloss>/g;
        let g;
        while ((g = glossRe.exec(body)) !== null) {
          const text = cleanText(g[1]);
          if (text && !glosses.includes(text)) glosses.push(text);
          if (glosses.length >= 6) break;
        }
        const gloss = glosses.join("; ");
        if (word) map.set(id, { word, gloss });
      }
      abbottIndex = map;
      return map;
    })();
  }
  return abbottPromise;
}
async function loadGreekStrong() {
  if (greekStrongIndex) return greekStrongIndex;
  if (!greekStrongPromise) {
    greekStrongPromise = (async () => {
      const file = path3.join(
        REFERENCE_ROOT,
        "strongs",
        "greek",
        "strongs-greek-dictionary.js"
      );
      const raw = await fs3.readFile(file, "utf8");
      const map = /* @__PURE__ */ new Map();
      const start = raw.indexOf("{");
      const end = raw.lastIndexOf("}");
      if (start >= 0 && end > start) {
        const json = raw.slice(start, end + 1);
        try {
          const parsed = JSON.parse(json);
          for (const [id, entry] of Object.entries(parsed)) {
            map.set(id, {
              lemma: entry.lemma ?? "",
              translit: entry.translit ?? "",
              def: collapseWhitespace(entry.strongs_def ?? "")
            });
          }
        } catch {
        }
      }
      greekStrongIndex = map;
      return map;
    })();
  }
  return greekStrongPromise;
}
function transliterateGreek(word) {
  const lower = word.normalize("NFD").replace(/[̀-ͯ]/g, "");
  let out = "";
  for (const ch of lower.toLowerCase()) {
    out += GREEK_TRANSLIT_MAP[ch] ?? (/[a-z]/.test(ch) ? ch : "");
  }
  return out;
}
async function buildHebrewStudy(strong, englishWord) {
  const strongMap = await loadHebrewStrong();
  const strongEntry = strongMap.get(strong);
  if (!strongEntry) return null;
  const numeric = strong.replace(/^H/, "");
  const xref = await loadBdbXref();
  const bdbId = xref.get(numeric);
  let gloss = "";
  let source = "";
  if (bdbId) {
    const bdbEntries = await loadBdbEntries();
    const bdbGloss = bdbEntries.get(bdbId);
    if (bdbGloss) {
      gloss = limitGloss(bdbGloss);
      source = `Brown-Driver-Briggs (Strong's ${strong})`;
    }
  }
  if (!gloss) {
    gloss = limitGloss(strongEntry.meaning);
    source = `Strong's Hebrew (${strong})`;
  }
  if (!gloss) return null;
  return {
    word: strongEntry.word,
    xlit: strongEntry.xlit,
    strong,
    gloss,
    source,
    ...englishWord ? { englishWord } : {}
  };
}
async function buildGreekStudy(strong, fallbackWord, englishWord) {
  const abbott = await loadAbbottSmith();
  const abbottEntry = abbott.get(strong);
  const greekStrong = await loadGreekStrong();
  const strongEntry = greekStrong.get(strong);
  let word = abbottEntry?.word || strongEntry?.lemma || fallbackWord;
  let xlit = strongEntry?.translit ? decodeEntities(strongEntry.translit) : "";
  if (!xlit) xlit = transliterateGreek(word);
  let gloss = "";
  let source = "";
  if (abbottEntry && abbottEntry.gloss) {
    gloss = limitGloss(abbottEntry.gloss);
    source = `Abbott-Smith Manual Greek Lexicon (Strong's ${strong})`;
  } else if (strongEntry && strongEntry.def) {
    gloss = limitGloss(strongEntry.def);
    source = `Strong's Greek (${strong})`;
  }
  if (!gloss) return null;
  return {
    word,
    xlit,
    strong,
    gloss,
    source,
    ...englishWord ? { englishWord } : {}
  };
}
async function loadHebrewBook(osis) {
  const cached = hebrewBookCache.get(osis);
  if (cached) return cached;
  const file = path3.join(REFERENCE_ROOT, "morphhb", "wlc", `${osis}.xml`);
  const raw = await fs3.readFile(file, "utf8");
  hebrewBookCache.set(osis, raw);
  return raw;
}
async function loadHebrewFrequency() {
  if (hebrewFreqIndex) return hebrewFreqIndex;
  if (!hebrewFreqPromise) {
    hebrewFreqPromise = (async () => {
      const freq = /* @__PURE__ */ new Map();
      const lemmaRe = /\blemma="([^"]+)"/g;
      for (const osis of Object.values(HEBREW_OSIS)) {
        const xml = await loadHebrewBook(osis);
        let m;
        while ((m = lemmaRe.exec(xml)) !== null) {
          const strong = hebrewStrongFromLemma(m[1]);
          if (!strong) continue;
          freq.set(strong, (freq.get(strong) ?? 0) + 1);
        }
      }
      hebrewFreqIndex = freq;
      return freq;
    })();
  }
  return hebrewFreqPromise;
}
function extractHebrewVerseWords(bookXml, osis, chapter, verse) {
  const targetKjv = `${osis}.${chapter}.${verse}`;
  const verseRe = /<verse osisID="([^"]+)">([\s\S]*?)<\/verse>/g;
  let m;
  let matchedBody = null;
  let kjvFallbackBody = null;
  while ((m = verseRe.exec(bookXml)) !== null) {
    const osisId = m[1];
    const body2 = m[2];
    const noteMatch = body2.match(/<note>KJV:([^<]+)<\/note>/);
    if (noteMatch) {
      const kjvRef = noteMatch[1].trim();
      if (kjvRef === targetKjv) {
        matchedBody = body2;
        break;
      }
    } else if (osisId === targetKjv) {
      kjvFallbackBody = body2;
    }
  }
  const body = matchedBody ?? kjvFallbackBody;
  if (!body) return [];
  const out = [];
  const wRe = /<w\b([^>]*)\blemma="([^"]+)"([^>]*)>([\s\S]*?)<\/w>/g;
  let w;
  while ((w = wRe.exec(body)) !== null) {
    const allAttrs = `${w[1]} ${w[3]}`;
    const lemma = w[2];
    const morphMatch = allAttrs.match(/\bmorph="([^"]+)"/);
    const morph = morphMatch ? morphMatch[1] : "";
    const surface = cleanText(w[4]);
    const strong = hebrewStrongFromLemma(lemma);
    if (!strong) continue;
    out.push({ strong, word: surface, morph });
  }
  return out;
}
async function loadTagntFile(filePath) {
  const cached = greekFileCache.get(filePath);
  if (cached) return cached;
  const raw = await fs3.readFile(filePath, "utf8");
  greekFileCache.set(filePath, raw);
  return raw;
}
function extractGreekVerseWords(fileText, osis, chapter, verse) {
  const prefix = `${osis}.${chapter}.${verse}#`;
  const out = [];
  const lines = fileText.split("\n");
  for (const line of lines) {
    if (!line.startsWith(prefix)) continue;
    const cols = line.split("	");
    if (cols.length < 4) continue;
    const surfaceCol = cols[1] ?? "";
    const surfaceMatch = surfaceCol.match(/^([^(]*)\(([^)]*)\)/);
    const word = collapseWhitespace(
      surfaceMatch ? surfaceMatch[1] : surfaceCol
    ).replace(/[,.;·:]+$/, "");
    const translit = surfaceMatch ? surfaceMatch[2].trim() : "";
    const english = collapseWhitespace((cols[2] ?? "").replace(/[<>]/g, ""));
    const tag = cols[3] ?? "";
    const tagMatch = tag.match(/^(G\d+)=([A-Z0-9-]+)/);
    if (!tagMatch) continue;
    const strong = `G${parseInt(tagMatch[1].slice(1), 10)}`;
    const morph = tagMatch[2];
    out.push({ strong, word, translit, english, morph });
  }
  return out;
}
async function getWordStudiesForVerse(reference, max = 3) {
  const parsed = parseReference(reference);
  if (!parsed) {
    throw new Error(`Could not parse Scripture reference: "${reference}"`);
  }
  const versesData = await loadVersesIndex();
  if (versesData) {
    const strongs = await loadStrongsIndex();
    if (!strongs) {
      throw new Error(
        "Precomputed verse index present but Strong\u2019s index missing. Re-run `npm run build:lexicon-index`."
      );
    }
    const tuples = versesData.verses[parsed.canonical];
    if (!tuples) {
      throw new Error(
        `No precomputed word studies for ${parsed.canonical}. Verse may be untaggable (OT non-Hebrew / NT non-Greek) or outside corpus.`
      );
    }
    return entriesToWordStudies(tuples, versesData.pool, strongs, max);
  }
  if (forcePrecomputed) {
    throw new Error(
      "Precomputed lexicon index missing and forcePrecomputed is set. Run `npm run build:lexicon-index` to generate public/lexicon-verses.json."
    );
  }
  const meta = BIBLE_BOOK_META[parsed.book];
  const chapter = parsed.startChapter;
  const verse = parsed.startVerse === 0 ? 1 : parsed.startVerse;
  if (meta.testament === "OT") {
    return getHebrewVerseStudies(parsed, chapter, verse, max);
  }
  return getGreekVerseStudies(parsed, chapter, verse, max);
}
function entriesToWordStudies(tuples, pool, strongs, max) {
  const out = [];
  for (const t of tuples) {
    if (out.length >= max) break;
    const e = tupleToEntry(t, pool);
    const dict = strongs[e.s];
    if (!dict) continue;
    out.push({
      word: e.w || dict.word,
      xlit: dict.xlit || e.x || "",
      strong: e.s,
      gloss: dict.gloss,
      source: dict.source,
      ...e.e ? { englishWord: e.e } : {}
    });
  }
  return out;
}
async function computeHebrewVerseEntries(parsed, chapter, verse) {
  const osis = HEBREW_OSIS[parsed.book];
  if (!osis) {
    throw new Error(
      `No morphHB mapping for OT book ${parsed.book} (${parsed.bookName})`
    );
  }
  const bookXml = await loadHebrewBook(osis);
  const tagged = extractHebrewVerseWords(bookXml, osis, chapter, verse);
  if (tagged.length === 0) return [];
  const candidates = [];
  const seenStrong = /* @__PURE__ */ new Set();
  for (const t of tagged) {
    if (seenStrong.has(t.strong)) continue;
    if (!isHebrewContentWord(t.morph, t.strong)) continue;
    seenStrong.add(t.strong);
    candidates.push(t);
  }
  const freq = await loadHebrewFrequency();
  const ranked = candidates.map((t, index) => ({ t, index, freq: freq.get(t.strong) ?? Infinity })).sort((a, b) => a.freq - b.freq || a.index - b.index);
  const out = [];
  for (const { t } of ranked) {
    const study = await buildHebrewStudy(t.strong);
    if (!study) continue;
    out.push({ s: t.strong, w: t.word || study.word });
  }
  return out;
}
async function getHebrewVerseStudies(parsed, chapter, verse, max) {
  const entries = await computeHebrewVerseEntries(parsed, chapter, verse);
  if (entries.length === 0) {
    const osis = HEBREW_OSIS[parsed.book];
    throw new Error(
      `No tagged Hebrew words found for ${parsed.canonical} (WLC ${osis}.${chapter}.${verse}). Check versification.`
    );
  }
  const results = [];
  for (const e of entries) {
    if (results.length >= max) break;
    const study = await buildHebrewStudy(e.s);
    if (!study) continue;
    results.push({ ...study, word: e.w || study.word });
  }
  return results;
}
async function computeGreekVerseEntries(parsed, chapter, verse) {
  const osis = GREEK_OSIS[parsed.book];
  if (!osis) {
    throw new Error(
      `No TAGNT mapping for NT book ${parsed.book} (${parsed.bookName})`
    );
  }
  const fileText = await loadTagntFile(tagntFileForBook(osis));
  const tagged = extractGreekVerseWords(fileText, osis, chapter, verse);
  if (tagged.length === 0) return [];
  const out = [];
  const seen = /* @__PURE__ */ new Set();
  for (const t of tagged) {
    if (seen.has(t.strong)) continue;
    if (!isGreekContentMorph(t.morph)) continue;
    const study = await buildGreekStudy(
      t.strong,
      t.word,
      t.english || void 0
    );
    if (!study) continue;
    seen.add(t.strong);
    const entry = {
      s: t.strong,
      w: t.word || study.word
    };
    if (t.translit) entry.x = t.translit;
    if (t.english) entry.e = t.english;
    out.push(entry);
  }
  return out;
}
async function getGreekVerseStudies(parsed, chapter, verse, max) {
  const entries = await computeGreekVerseEntries(parsed, chapter, verse);
  if (entries.length === 0) {
    const osis = GREEK_OSIS[parsed.book];
    throw new Error(
      `No tagged Greek words found for ${parsed.canonical} (TAGNT ${osis}.${chapter}.${verse}).`
    );
  }
  const results = [];
  for (const e of entries) {
    if (results.length >= max) break;
    const study = await buildGreekStudy(e.s, e.w, e.e);
    if (!study) continue;
    results.push({
      ...study,
      word: e.w || study.word,
      xlit: study.xlit || e.x || ""
    });
  }
  return results;
}
var STRONGS_INDEX_ASSET, VERSES_INDEX_ASSET, forcePrecomputed, strongsIndex, strongsIndexPromise, versesIndex, versesIndexPromise, REFERENCE_ROOT, STEPBIBLE_ROOT, HEBREW_OSIS, GREEK_OSIS, GREEK_TAGNT_MAT_JHN, HEBREW_CONTENT_POS, hebrewStrongIndex, hebrewStrongPromise, bdbXrefIndex, bdbXrefPromise, bdbEntryIndex, bdbEntryPromise, abbottIndex, abbottPromise, greekStrongIndex, greekStrongPromise, GREEK_TRANSLIT_MAP, hebrewBookCache, hebrewFreqIndex, hebrewFreqPromise, greekFileCache;
var init_lexicon = __esm({
  "src/lib/soul-audit/lexicon.ts"() {
    "use strict";
    init_parseReference();
    init_books();
    STRONGS_INDEX_ASSET = "lexicon-strongs.json";
    VERSES_INDEX_ASSET = "lexicon-verses.json";
    forcePrecomputed = false;
    strongsIndex = null;
    strongsIndexPromise = null;
    versesIndex = null;
    versesIndexPromise = null;
    REFERENCE_ROOT = path3.join(
      process.cwd(),
      "content",
      "reference",
      "lexicons"
    );
    STEPBIBLE_ROOT = path3.join(
      process.cwd(),
      "content",
      "reference",
      "stepbible-data",
      "STEPBible-Data",
      "Translators Amalgamated OT+NT"
    );
    HEBREW_OSIS = {
      GEN: "Gen",
      EXO: "Exod",
      LEV: "Lev",
      NUM: "Num",
      DEU: "Deut",
      JOS: "Josh",
      JDG: "Judg",
      RUT: "Ruth",
      "1SA": "1Sam",
      "2SA": "2Sam",
      "1KI": "1Kgs",
      "2KI": "2Kgs",
      "1CH": "1Chr",
      "2CH": "2Chr",
      EZR: "Ezra",
      NEH: "Neh",
      EST: "Esth",
      JOB: "Job",
      PSA: "Ps",
      PRO: "Prov",
      ECC: "Eccl",
      SNG: "Song",
      ISA: "Isa",
      JER: "Jer",
      LAM: "Lam",
      EZK: "Ezek",
      DAN: "Dan",
      HOS: "Hos",
      JOL: "Joel",
      AMO: "Amos",
      OBA: "Obad",
      JON: "Jonah",
      MIC: "Mic",
      NAM: "Nah",
      HAB: "Hab",
      ZEP: "Zeph",
      HAG: "Hag",
      ZEC: "Zech",
      MAL: "Mal"
    };
    GREEK_OSIS = {
      MAT: "Mat",
      MRK: "Mrk",
      LUK: "Luk",
      JHN: "Jhn",
      ACT: "Act",
      ROM: "Rom",
      "1CO": "1Co",
      "2CO": "2Co",
      GAL: "Gal",
      EPH: "Eph",
      PHP: "Php",
      COL: "Col",
      "1TH": "1Th",
      "2TH": "2Th",
      "1TI": "1Ti",
      "2TI": "2Ti",
      TIT: "Tit",
      PHM: "Phm",
      HEB: "Heb",
      JAS: "Jas",
      "1PE": "1Pe",
      "2PE": "2Pe",
      "1JN": "1Jn",
      "2JN": "2Jn",
      "3JN": "3Jn",
      JUD: "Jud",
      REV: "Rev"
    };
    GREEK_TAGNT_MAT_JHN = /* @__PURE__ */ new Set(["Mat", "Mrk", "Luk", "Jhn"]);
    HEBREW_CONTENT_POS = /* @__PURE__ */ new Set(["V", "N", "A"]);
    hebrewStrongIndex = null;
    hebrewStrongPromise = null;
    bdbXrefIndex = null;
    bdbXrefPromise = null;
    bdbEntryIndex = null;
    bdbEntryPromise = null;
    abbottIndex = null;
    abbottPromise = null;
    greekStrongIndex = null;
    greekStrongPromise = null;
    GREEK_TRANSLIT_MAP = {
      \u03B1: "a",
      \u03B2: "b",
      \u03B3: "g",
      \u03B4: "d",
      \u03B5: "e",
      \u03B6: "z",
      \u03B7: "e",
      \u03B8: "th",
      \u03B9: "i",
      \u03BA: "k",
      \u03BB: "l",
      \u03BC: "m",
      \u03BD: "n",
      \u03BE: "x",
      \u03BF: "o",
      \u03C0: "p",
      \u03C1: "r",
      \u03C3: "s",
      \u03C2: "s",
      \u03C4: "t",
      \u03C5: "y",
      \u03C6: "ph",
      \u03C7: "ch",
      \u03C8: "ps",
      \u03C9: "o"
    };
    hebrewBookCache = /* @__PURE__ */ new Map();
    hebrewFreqIndex = null;
    hebrewFreqPromise = null;
    greekFileCache = /* @__PURE__ */ new Map();
  }
});

// src/data/wordnote-bank.ts
var WORDNOTE_BANK;
var init_wordnote_bank = __esm({
  "src/data/wordnote-bank.ts"() {
    "use strict";
    WORDNOTE_BANK = [
      {
        id: "shalom",
        term: "peace",
        strong: "H7965",
        word: "\u05E9\u05C1\u05B8\u05DC\u05D5\u05B9\u05DD",
        xlit: "sh\xE2l\xF4wm",
        gloss: "completeness; soundness; welfare; peace; safety; health; prosperity; quiet",
        source: "Brown-Driver-Briggs (Strong's H7965)"
      },
      {
        id: "chesed",
        term: "steadfast love",
        strong: "H2617",
        word: "\u05D7\u05B6\u05E1\u05B6\u05D3",
        xlit: "che\xE7ed",
        gloss: "goodness; kindness; mercy; affection; love to God; piety; lovely appearance; lovingkindness",
        source: "Brown-Driver-Briggs (Strong's H2617)"
      },
      {
        id: "ruach",
        term: "spirit",
        strong: "H7307",
        word: "\u05E8\u05D5\u05BC\u05D7\u05B7",
        xlit: "r\xFBwach",
        gloss: "breath; wind; spirit; temper; disposition; spirit of God",
        source: "Brown-Driver-Briggs (Strong's H7307)"
      },
      {
        id: "nephesh",
        term: "soul",
        strong: "H5315",
        word: "\u05E0\u05B6\u05E4\u05B6\u05E9\u05C1",
        xlit: "nephesh",
        gloss: "soul; living being; life; self; person; desire; appetite; emotion",
        source: "Brown-Driver-Briggs (Strong's H5315)"
      },
      {
        id: "emunah",
        term: "faithfulness",
        strong: "H530",
        word: "\u05D0\u05B1\u05DE\u05D5\u05BC\u05E0\u05B8\u05D4",
        xlit: "\u02BC\u0115m\xFBwn\xE2h",
        gloss: "firmness; steadfastness; fidelity; steadiness; faithfulness; trust",
        source: "Brown-Driver-Briggs (Strong's H530)"
      },
      {
        id: "bara",
        term: "create",
        strong: "H1254",
        word: "\u05D1\u05BC\u05B8\u05E8\u05B8\u05D0",
        xlit: "b\xE2r\xE2\u02BC",
        gloss: "shape; create; fashion; be created; cut down; cut out",
        source: "Brown-Driver-Briggs (Strong's H1254)"
      },
      {
        id: "tehom",
        term: "the deep",
        strong: "H8415",
        word: "\u05EA\u05BC\u05B0\u05D4\u05D5\u05B9\u05DD",
        xlit: "t\u1D49h\xF4wm",
        gloss: "deep; sea; abyss; primaeval ocean; depth",
        source: "Brown-Driver-Briggs (Strong's H8415)"
      },
      {
        id: "kavod",
        term: "glory",
        strong: "H3519",
        word: "\u05DB\u05BC\u05B8\u05D1\u05D5\u05B9\u05D3",
        xlit: "k\xE2b\xF4wd",
        gloss: "abundance; honour; glory; riches; splendor; dignity; reputation; my honour",
        source: "Brown-Driver-Briggs (Strong's H3519)"
      },
      {
        id: "torah",
        term: "instruction",
        strong: "H8451",
        word: "\u05EA\u05BC\u05D5\u05B9\u05E8\u05B8\u05D4",
        xlit: "t\xF4wr\xE2h",
        gloss: "direction; instruction; law; custom; manner",
        source: "Brown-Driver-Briggs (Strong's H8451)"
      },
      {
        id: "berit",
        term: "covenant",
        strong: "H1285",
        word: "\u05D1\u05BC\u05B0\u05E8\u05B4\u05D9\u05EA",
        xlit: "b\u1D49r\xEEyth",
        gloss: "covenant; treaty; alliance; league; constitution; ordinance; agreement; pledge",
        source: "Brown-Driver-Briggs (Strong's H1285)"
      },
      {
        id: "qavah",
        term: "wait",
        strong: "H6960",
        word: "\u05E7\u05B8\u05D5\u05B8\u05D4",
        xlit: "q\xE2v\xE2h",
        gloss: "wait for; those waiting for; wait; look eagerly; for; lie in wait for",
        source: "Brown-Driver-Briggs (Strong's H6960)"
      },
      {
        id: "nacham",
        term: "comfort",
        strong: "H5162",
        word: "\u05E0\u05B8\u05D7\u05B7\u05DD",
        xlit: "n\xE2cham",
        gloss: "be sorry; console oneself; moved to pity; have compassion; rue; suffer grief; repent; comfort oneself",
        source: "Brown-Driver-Briggs (Strong's H5162)"
      },
      {
        id: "yare",
        term: "fear",
        strong: "H3372",
        word: "\u05D9\u05B8\u05E8\u05B5\u05D0",
        xlit: "y\xE2r\xEA\u02BC",
        gloss: "fear; be afraid; stand in awe of; reverence; honour; be fearful; dreadful; cause astonishment and awe",
        source: "Brown-Driver-Briggs (Strong's H3372)"
      },
      {
        id: "yada",
        term: "know",
        strong: "H3045",
        word: "\u05D9\u05B8\u05D3\u05B7\u05E2",
        xlit: "y\xE2da\u02BB",
        gloss: "know; learn to know; perceive; perceive and see; find out and discern; discriminate; distinguish; know by experience",
        source: "Brown-Driver-Briggs (Strong's H3045)"
      },
      {
        id: "shema",
        term: "hear",
        strong: "H8085",
        word: "\u05E9\u05C1\u05B8\u05DE\u05B7\u05E2",
        xlit: "sh\xE2ma\u02BB",
        gloss: "hear; be heard; be heard of; be regarded; obeyed; hearing was granted to their voice; he caused; to hear",
        source: "Brown-Driver-Briggs (Strong's H8085)"
      },
      {
        id: "tsedeq",
        term: "righteousness",
        strong: "H6664",
        word: "\u05E6\u05B6\u05D3\u05B6\u05E7",
        xlit: "tsedeq",
        gloss: "rightness; righteousness; what is right; just; normal; justness; justice; justification",
        source: "Brown-Driver-Briggs (Strong's H6664)"
      },
      {
        id: "emet",
        term: "truth",
        strong: "H571",
        word: "\u05D0\u05B6\u05DE\u05B6\u05EA",
        xlit: "\u02BCemeth",
        gloss: "firmness; faithfulness; reliability; sureness; stability; continuance; reliableness; truth",
        source: "Brown-Driver-Briggs (Strong's H571)"
      },
      {
        id: "raphah",
        term: "be still",
        strong: "H7503",
        word: "\u05E8\u05B8\u05E4\u05B8\u05D4",
        xlit: "r\xE2ph\xE2h",
        gloss: "sink; relax; sink down; drop; abate; withdraw; idle; let",
        source: "Brown-Driver-Briggs (Strong's H7503)"
      },
      {
        id: "shuv",
        term: "return",
        strong: "H7725",
        word: "\u05E9\u05C1\u05D5\u05BC\u05D1",
        xlit: "sh\xFBwb",
        gloss: "turn back; return; come; go back; return unto; turn; apostatize; turn away",
        source: "Brown-Driver-Briggs (Strong's H7725)"
      },
      {
        id: "or",
        term: "light",
        strong: "H216",
        word: "\u05D0\u05D5\u05B9\u05E8",
        xlit: "\u02BC\xF4wr",
        gloss: "light; morning light; dawn; daylight; lightning; light of life; light of prosperity; light of instruction",
        source: "Brown-Driver-Briggs (Strong's H216)"
      },
      {
        id: "agape",
        term: "love",
        strong: "G26",
        word: "\u1F00\u03B3\u03AC\u03C0\u03B7",
        xlit: "ag\xE1p\u0113",
        gloss: "love; goodwill; esteem; love feasts",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G26)"
      },
      {
        id: "agapao",
        term: "to love",
        strong: "G25",
        word: "\u1F00\u03B3\u03B1\u03C0\u03AC\u03C9",
        xlit: "agap\xE1\u014D",
        gloss: "to love",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G25)"
      },
      {
        id: "charis",
        term: "grace",
        strong: "G5485",
        word: "\u03C7\u03AC\u03C1\u03B9\u03C2",
        xlit: "ch\xE1ris",
        gloss: "gracefulness, grace, loveliness; graciousness; grace, graciousness, kindness, goodwill, favour; favour, grace; thanks, gratitude; to be thankful",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G5485)"
      },
      {
        id: "pistis",
        term: "faith",
        strong: "G4102",
        word: "\u03C0\u03AF\u03C3\u03C4\u03B9\u03C2",
        xlit: "p\xEDstis",
        gloss: "faith; belief; trust; confidence; the faith; fidelity",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G4102)"
      },
      {
        id: "elpis",
        term: "hope",
        strong: "G1680",
        word: "\u1F10\u03BB\u03C0\u03AF\u03C2",
        xlit: "elp\xEDs",
        gloss: "expectation; hope",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G1680)"
      },
      {
        id: "logos",
        term: "word",
        strong: "G3056",
        word: "\u03BB\u03CC\u03B3\u03BF\u03C2",
        xlit: "l\xF3gos",
        gloss: "a word; a saying; statement; declaration; the word of God; speech",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G3056)"
      },
      {
        id: "pneuma",
        term: "spirit",
        strong: "G4151",
        word: "\u03C0\u03BD\u03B5\u1FE6\u03BC\u03B1",
        xlit: "pne\xFBma",
        gloss: "wind; breath; the spirit; in spirit; spirit; a spirit",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G4151)"
      },
      {
        id: "chara",
        term: "joy",
        strong: "G5479",
        word: "\u03C7\u03B1\u03C1\u03AC",
        xlit: "char\xE1",
        gloss: "joy, delight",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G5479)"
      },
      {
        id: "eirene",
        term: "peace",
        strong: "G1515",
        word: "\u03B5\u1F30\u03C1\u03AE\u03BD\u03B7",
        xlit: "eir\u1E17n\u0113",
        gloss: "peace",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G1515)"
      },
      {
        id: "metanoia",
        term: "repentance",
        strong: "G3341",
        word: "\u03BC\u03B5\u03C4\u03AC\u03BD\u03BF\u03B9\u03B1",
        xlit: "met\xE1noia",
        gloss: "after-thought; change of mind; repentance",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G3341)"
      },
      {
        id: "hamartia",
        term: "sin",
        strong: "G266",
        word: "\u1F01\u03BC\u03B1\u03C1\u03C4\u03AF\u03B1",
        xlit: "hamart\xEDa",
        gloss: "a missing the mark; guilt; sin; a fault, failure; a sinning, sin; a sinful deed",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G266)"
      },
      {
        id: "dikaiosyne",
        term: "righteousness",
        strong: "G1343",
        word: "\u03B4\u03B9\u03BA\u03B1\u03B9\u03BF\u03C3\u03CD\u03BD\u03B7",
        xlit: "dikaios\xFDn\u0113",
        gloss: "righteousness; teaching of; justice",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G1343)"
      },
      {
        id: "soteria",
        term: "salvation",
        strong: "G4991",
        word: "\u03C3\u03C9\u03C4\u03B7\u03C1\u03AF\u03B1",
        xlit: "s\u014Dt\u0113r\xEDa",
        gloss: "deliverance, preservation, salvation, safety",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G4991)"
      },
      {
        id: "sozo",
        term: "to save",
        strong: "G4982",
        word: "\u03C3\u03CE\u03B6\u03C9",
        xlit: "s\u1E53z\u014D",
        gloss: "to save",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G4982)"
      },
      {
        id: "koinonia",
        term: "fellowship",
        strong: "G2842",
        word: "\u03BA\u03BF\u03B9\u03BD\u03C9\u03BD\u03AF\u03B1",
        xlit: "koin\u014Dn\xEDa",
        gloss: "fellowship; communion; contribution",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G2842)"
      },
      {
        id: "doxa",
        term: "glory",
        strong: "G1391",
        word: "\u03B4\u03CC\u03BE\u03B1",
        xlit: "d\xF3xa",
        gloss: "expectation, judgment, opinion; opinion, estimation; repute; good opinion; reputation, praise, honour, glory; brightness, splendour, glory",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G1391)"
      },
      {
        id: "aletheia",
        term: "truth",
        strong: "G225",
        word: "\u1F00\u03BB\u03AE\u03B8\u03B5\u03B9\u03B1",
        xlit: "al\u1E17theia",
        gloss: "truth; truthfulness, truth",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G225)"
      },
      {
        id: "makarios",
        term: "blessed",
        strong: "G3107",
        word: "\u03BC\u03B1\u03BA\u03AC\u03C1\u03B9\u03BF\u03C2",
        xlit: "mak\xE1rios",
        gloss: "blessed; happy",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G3107)"
      },
      {
        id: "parakletos",
        term: "advocate",
        strong: "G3875",
        word: "\u03C0\u03B1\u03C1\u03AC\u03BA\u03BB\u03B7\u03C4\u03BF\u03C2",
        xlit: "par\xE1kl\u0113tos",
        gloss: "called to one's aid; an advocate; pleader; intercessor",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G3875)"
      },
      {
        id: "paraklesis",
        term: "comfort",
        strong: "G3874",
        word: "\u03C0\u03B1\u03C1\u03AC\u03BA\u03BB\u03B7\u03C3\u03B9\u03C2",
        xlit: "par\xE1kl\u0113sis",
        gloss: "a calling to one's aid; summons; appeal; entreaty; exhortation; encouragement",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G3874)"
      },
      {
        id: "hupomone",
        term: "endurance",
        strong: "G5281",
        word: "\u1F51\u03C0\u03BF\u03BC\u03BF\u03BD\u03AE",
        xlit: "hypomon\u1E17",
        gloss: "a remaining behind; patient enduring, endurance",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G5281)"
      },
      {
        id: "kurios",
        term: "Lord",
        strong: "G2962",
        word: "\u03BA\u03CD\u03C1\u03B9\u03BF\u03C2",
        xlit: "k\xFDrios",
        gloss: "having power; authority; lord; master",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G2962)"
      },
      {
        id: "euangelion",
        term: "good news",
        strong: "G2098",
        word: "\u03B5\u1F50\u03B1\u03B3\u03B3\u03AD\u03BB\u03B9\u03BF\u03BD",
        xlit: "euang\xE9lion",
        gloss: "a reward for good tidings; to make a thank-offering for good tidings; good tidings, good news; the gospel",
        source: "Abbott-Smith Manual Greek Lexicon (Strong's G2098)"
      }
    ];
  }
});

// src/lib/brain/cost.ts
function estimateCostUsd(params) {
  const profile = COSTS[params.provider];
  const inputCost = Math.max(0, params.inputTokens) / 1e6 * profile.inputPerMillionUsd;
  const outputCost = Math.max(0, params.outputTokens) / 1e6 * profile.outputPerMillionUsd;
  return Number((inputCost + outputCost).toFixed(8));
}
function estimateInputTokens(text) {
  return Math.max(1, Math.ceil(text.length / 4));
}
function estimateOutputTokens(text) {
  return Math.max(1, Math.ceil(text.length / 4));
}
function providerBaseCostRank(provider) {
  const profile = COSTS[provider];
  return profile.inputPerMillionUsd + profile.outputPerMillionUsd;
}
var COSTS;
var init_cost = __esm({
  "src/lib/brain/cost.ts"() {
    "use strict";
    COSTS = {
      // Conservative defaults aligned with low-cost routing goals.
      openai: {
        inputPerMillionUsd: 0.05,
        outputPerMillionUsd: 0.4
      },
      google: {
        inputPerMillionUsd: 0.075,
        outputPerMillionUsd: 0.3
      },
      minimax: {
        inputPerMillionUsd: 0.3,
        outputPerMillionUsd: 1.2
      },
      nvidia_kimi: {
        inputPerMillionUsd: 0.2,
        outputPerMillionUsd: 0.8
      }
    };
  }
});

// src/lib/brain/flags.ts
function toBool(value, fallback) {
  if (value === void 0) return fallback;
  const normalized = value.trim().toLowerCase();
  if (["1", "true", "yes", "on"].includes(normalized)) return true;
  if (["0", "false", "no", "off"].includes(normalized)) return false;
  return fallback;
}
function toNumber(value, fallback) {
  if (!value) return fallback;
  const parsed = Number.parseFloat(value);
  return Number.isFinite(parsed) ? parsed : fallback;
}
var brainFlags;
var init_flags = __esm({
  "src/lib/brain/flags.ts"() {
    "use strict";
    brainFlags = {
      // ─── Soul Audit Controls ──────────────────────────────────────────
      /** Emergency kill switch — disables Soul Audit entirely */
      soulAuditEnabled: toBool(process.env.SOUL_AUDIT_ENABLED, true),
      /** Enable clarifier-once for vague inputs */
      clarifierEnabled: toBool(process.env.CLARIFIER_ENABLED, true),
      // ─── Brain Router ─────────────────────────────────────────────────
      brainRouterEnabled: toBool(process.env.BRAIN_ROUTER_ENABLED, true),
      brainSwitchUiEnabled: toBool(process.env.BRAIN_SWITCH_UI_ENABLED, true),
      openWebModeEnabled: toBool(process.env.OPEN_WEB_MODE_ENABLED, true),
      chatStudySidebarEnabled: toBool(
        process.env.CHAT_STUDY_SIDEBAR_V2_ENABLED,
        true
      ),
      qualityFloor: toNumber(process.env.BRAIN_QUALITY_FLOOR, 0.65),
      freeChatMonthlyCap: Math.max(
        1,
        Math.round(toNumber(process.env.FREE_CHAT_MONTHLY_CAP, 20))
      ),
      subscriptionChatMonthlyCredit: Math.max(
        1,
        Math.round(toNumber(process.env.SUBSCRIPTION_CHAT_MONTHLY_CREDIT, 1e3))
      ),
      platformMonthlyBudgetUsd: Math.max(
        1,
        toNumber(process.env.PLATFORM_AI_BUDGET_MONTHLY_USD, 100)
      ),
      nearLimitThreshold: toNumber(process.env.CHAT_NEAR_LIMIT_THRESHOLD, 0.8),
      // ─── Token Optimization Flags ────────────────────────────────────
      // Defaults are cost-optimized. Set to true for premium/higher quality.
      /** Use LLM for Sabbath/Review days (default: false = deterministic templates) */
      generativeSabbathReview: toBool(process.env.GENERATIVE_SABBATH_REVIEW, false),
      /** Use LLM for intent parsing (default: false = deterministic keyword parser) */
      llmIntentParsing: toBool(process.env.LLM_INTENT_PARSING, false),
      /** Use LLM doc reranking in polishing (default: false = keyword ordering) */
      llmDocReranking: toBool(process.env.LLM_DOC_RERANKING, false),
      /** Max reference chunks per devotional day (default: 4, was 6-12) */
      maxReferenceChunksPerDay: Math.max(
        2,
        Math.round(toNumber(process.env.MAX_REFERENCE_CHUNKS_PER_DAY, 4))
      ),
      /** Max characters per reference chunk in LLM context (default: 1200) */
      maxChunkCharsInContext: Math.max(
        400,
        Math.round(toNumber(process.env.MAX_CHUNK_CHARS_IN_CONTEXT, 1200))
      )
    };
  }
});

// src/lib/brain/health-store.ts
function healthStoreEnabled() {
  return process.env.BRAIN_HEALTH_KV_ENABLED === "on";
}
async function getKv() {
  try {
    const { getCloudflareContext: getCloudflareContext2 } = await Promise.resolve().then(() => (init_api(), api_exports));
    const { env } = await getCloudflareContext2({ async: true });
    const candidate = env.BRAIN_HEALTH_KV;
    return candidate ?? null;
  } catch {
    return null;
  }
}
async function loadProviderHealth() {
  if (!healthStoreEnabled()) return null;
  const kv = await getKv();
  if (!kv) return null;
  try {
    const raw = await kv.get(KV_KEY);
    if (!raw) return null;
    const parsed = JSON.parse(raw);
    if (!parsed || typeof parsed !== "object") return null;
    return parsed;
  } catch {
    return null;
  }
}
async function persistProviderHealth(snapshot) {
  if (!healthStoreEnabled()) return;
  const kv = await getKv();
  if (!kv) return;
  try {
    await kv.put(KV_KEY, JSON.stringify(snapshot));
  } catch {
  }
}
var KV_KEY;
var init_health_store = __esm({
  "src/lib/brain/health-store.ts"() {
    "use strict";
    KV_KEY = "BRAIN_HEALTH_V1";
  }
});

// src/lib/brain/router.ts
async function ensureHealthHydrated() {
  if (healthHydrated || !healthStoreEnabled()) {
    healthHydrated = true;
    return;
  }
  if (healthHydrationPromise) return healthHydrationPromise;
  healthHydrationPromise = (async () => {
    const snapshot = await loadProviderHealth();
    if (snapshot) {
      for (const [provider, state] of Object.entries(snapshot)) {
        providerHealth.set(provider, state);
      }
    }
    healthHydrated = true;
  })();
  return healthHydrationPromise;
}
function maybePersistHealth() {
  if (!healthStoreEnabled()) return;
  const now = Date.now();
  if (now - lastHealthPersistAt < HEALTH_PERSIST_THROTTLE_MS) return;
  lastHealthPersistAt = now;
  const snapshot = {};
  for (const [provider, state] of providerHealth.entries()) {
    snapshot[provider] = state;
  }
  void persistProviderHealth(snapshot);
}
function parseRetryAfterMs(headerValue) {
  if (!headerValue) return null;
  const trimmed = headerValue.trim();
  if (!trimmed) return null;
  const seconds = Number(trimmed);
  if (Number.isFinite(seconds) && seconds >= 0) {
    return Math.min(seconds * 1e3, 3e4);
  }
  const date = Date.parse(trimmed);
  if (!Number.isNaN(date)) {
    return Math.max(0, Math.min(date - Date.now(), 3e4));
  }
  return null;
}
async function sleep(ms) {
  if (ms <= 0) return;
  await new Promise((resolve) => setTimeout(resolve, ms));
}
function normalizeProviderMode(mode) {
  if (mode === "openai" || mode === "google" || mode === "minimax" || mode === "nvidia_kimi") {
    return mode;
  }
  return "auto";
}
function qualityScore(text, minLength = 220) {
  const normalized = text.trim();
  if (!normalized) return 0;
  const lengthScore = Math.min(1, normalized.length / minLength);
  const scriptureRefPattern = /\b(?:[1-3]\s)?[A-Z][a-z]+\s\d{1,3}:\d{1,3}(?:-\d{1,3})?\b/;
  const scriptureScore = scriptureRefPattern.test(normalized) ? 0.2 : 0;
  const paragraphScore = normalized.split(/\n{2,}/).length >= 2 ? 0.2 : 0;
  return Math.max(
    0,
    Math.min(
      1,
      Number((lengthScore * 0.6 + paragraphScore + scriptureScore).toFixed(3))
    )
  );
}
function firstNonEmptyEnv(...names) {
  for (const name of names) {
    const value = process.env[name]?.trim();
    if (value && value.length > 0) return value;
  }
  return void 0;
}
function providerCredentials(provider, userKeys) {
  switch (provider) {
    case "openai":
      return {
        platformKey: firstNonEmptyEnv("ANTHROPIC_API_KEY", "OPENAI_API_KEY"),
        userKey: userKeys?.openai
      };
    case "google":
      return {
        platformKey: firstNonEmptyEnv("GOOGLE_API_KEY", "GEMINI_API_KEY"),
        userKey: userKeys?.google
      };
    case "minimax":
      return {
        platformKey: firstNonEmptyEnv("MINIMAX_API_KEY"),
        userKey: userKeys?.minimax
      };
    case "nvidia_kimi":
      return {
        platformKey: firstNonEmptyEnv("NVIDIA_KIMI_API_KEY"),
        userKey: userKeys?.nvidia_kimi
      };
  }
}
function resolvedApiKey(credentials) {
  if (credentials.userKey && credentials.userKey.trim().length > 0) {
    return { key: credentials.userKey.trim(), using: "byo_key" };
  }
  if (credentials.platformKey && credentials.platformKey.trim().length > 0) {
    return { key: credentials.platformKey.trim(), using: "platform_key" };
  }
  return { key: null, using: "unavailable" };
}
function providerAvailabilityForUser(params) {
  const allowHighCostOnPlatform = params.allowHighCostOnPlatform === true;
  const platformKeysEnabled = params.platformKeysEnabled !== false;
  const providerOrder = [
    "openai",
    "google",
    "minimax",
    "nvidia_kimi"
  ];
  return providerOrder.map((provider) => {
    const credentials = providerCredentials(provider, params.userKeys);
    const resolved = resolvedApiKey(credentials);
    if (resolved.using === "byo_key") {
      return {
        provider,
        available: true,
        using: "byo_key"
      };
    }
    const highCostProvider = provider === "minimax" || provider === "nvidia_kimi";
    if (highCostProvider && !allowHighCostOnPlatform) {
      if (resolved.using === "platform_key") {
        return {
          provider,
          available: false,
          using: "unavailable",
          reason: "Available with BYO key only under low-cost platform policy."
        };
      }
      return {
        provider,
        available: false,
        using: "unavailable",
        reason: "BYO key required."
      };
    }
    if (!resolved.key) {
      return {
        provider,
        available: false,
        using: "unavailable",
        reason: "No API key configured."
      };
    }
    if (!platformKeysEnabled && resolved.using === "platform_key") {
      return {
        provider,
        available: false,
        using: "unavailable",
        reason: "Platform-funded routing is currently halted."
      };
    }
    return {
      provider,
      available: true,
      using: resolved.using
    };
  });
}
function isAnthropicKey(value) {
  return value.startsWith("sk-ant-");
}
function sortByCheapest(providers) {
  return [...providers].sort(
    (a, b) => providerBaseCostRank(a) - providerBaseCostRank(b)
  );
}
function sortClaudeFirstThenCheapest(providers) {
  const unique = Array.from(new Set(providers));
  const hasClaude = unique.includes("openai");
  const rest = unique.filter((provider) => provider !== "openai");
  const cheapestRest = sortByCheapest(rest).sort((a, b) => {
    const aHealth = providerHealth.get(a);
    const bHealth = providerHealth.get(b);
    const aFailureRatio = aHealth ? aHealth.failures / Math.max(1, aHealth.successes + aHealth.failures) : 0;
    const bFailureRatio = bHealth ? bHealth.failures / Math.max(1, bHealth.successes + bHealth.failures) : 0;
    const aLatency = aHealth?.avgLatencyMs || 0;
    const bLatency = bHealth?.avgLatencyMs || 0;
    const aScore = aFailureRatio * 4 + aLatency / 4e3 + providerBaseCostRank(a) / 10;
    const bScore = bFailureRatio * 4 + bLatency / 4e3 + providerBaseCostRank(b) / 10;
    return aScore - bScore;
  });
  return hasClaude ? ["openai", ...cheapestRest] : cheapestRest;
}
function recordProviderHealth(params) {
  const current = providerHealth.get(params.provider) || {
    successes: 0,
    failures: 0,
    avgLatencyMs: 0
  };
  const total = current.successes + current.failures;
  const nextLatency = total === 0 ? params.latencyMs : Math.round(
    (current.avgLatencyMs * total + params.latencyMs) / (total + 1)
  );
  providerHealth.set(params.provider, {
    successes: current.successes + (params.success ? 1 : 0),
    failures: current.failures + (params.success ? 0 : 1),
    avgLatencyMs: nextLatency
  });
  maybePersistHealth();
}
async function callOpenAI(params) {
  const tokenField = OPENAI_MODEL.startsWith("gpt-5") || OPENAI_MODEL.startsWith("o") ? { max_completion_tokens: params.maxOutputTokens } : { max_tokens: params.maxOutputTokens };
  const response = await fetch(OPENAI_API_URL, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${params.apiKey}`
    },
    body: JSON.stringify({
      model: OPENAI_MODEL,
      messages: [
        { role: "system", content: params.system },
        ...params.messages
      ],
      ...tokenField
    }),
    signal: params.signal
  });
  if (!response.ok) {
    const payload2 = await response.text().catch(() => "");
    throw new Error(
      `OpenAI request failed (${response.status}): ${payload2.slice(0, 240)}`
    );
  }
  const payload = await response.json();
  const text = payload.choices?.[0]?.message?.content?.trim() || "";
  const usage = typeof payload.usage?.prompt_tokens === "number" && typeof payload.usage?.completion_tokens === "number" ? {
    inputTokens: payload.usage.prompt_tokens,
    outputTokens: payload.usage.completion_tokens
  } : void 0;
  return { text, usage };
}
function buildAnthropicSystem(system) {
  if (system.length < ANTHROPIC_CACHE_MIN_CHARS) return system;
  return [
    {
      type: "text",
      text: system,
      cache_control: { type: "ephemeral" }
    }
  ];
}
function buildAnthropicFirstUserContent(message, cacheableUserPrefix) {
  if (!cacheableUserPrefix) return message.content;
  if (cacheableUserPrefix.length < ANTHROPIC_CACHE_MIN_CHARS) {
    return `${cacheableUserPrefix}

${message.content}`;
  }
  return [
    {
      type: "text",
      text: cacheableUserPrefix,
      cache_control: { type: "ephemeral" }
    },
    {
      type: "text",
      text: message.content
    }
  ];
}
async function callAnthropic(params) {
  const firstUserIndex = params.messages.findIndex((m) => m.role === "user");
  const messages = params.messages.map((message, idx) => {
    if (idx === firstUserIndex) {
      return {
        role: message.role,
        content: buildAnthropicFirstUserContent(
          message,
          params.cacheableUserPrefix
        )
      };
    }
    return { role: message.role, content: message.content };
  });
  const requestBody = JSON.stringify({
    model: params.model || ANTHROPIC_MODEL,
    system: buildAnthropicSystem(params.system),
    max_tokens: params.maxOutputTokens,
    messages
  });
  let lastError = null;
  let response = null;
  for (let attempt = 1; attempt <= ANTHROPIC_RETRY_MAX_ATTEMPTS; attempt++) {
    response = await fetch(ANTHROPIC_API_URL, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "x-api-key": params.apiKey,
        "anthropic-version": "2023-06-01"
      },
      body: requestBody,
      signal: params.signal
    });
    if (response.ok) break;
    if (!ANTHROPIC_RETRYABLE_STATUS(response.status)) {
      const payload2 = await response.text().catch(() => "");
      throw new Error(
        `Anthropic request failed (${response.status}): ${payload2.slice(0, 240)}`
      );
    }
    if (attempt === ANTHROPIC_RETRY_MAX_ATTEMPTS) {
      const payload2 = await response.text().catch(() => "");
      lastError = new Error(
        `Anthropic request failed after ${attempt} attempts (${response.status}): ${payload2.slice(0, 240)}`
      );
      break;
    }
    const retryAfterMs = parseRetryAfterMs(response.headers.get("Retry-After"));
    const backoffMs = retryAfterMs ?? ANTHROPIC_RETRY_BASE_BACKOFF_MS * Math.pow(2, attempt - 1);
    console.info(
      `[anthropic-retry] status=${response.status} attempt=${attempt} backoff_ms=${backoffMs}`
    );
    await sleep(backoffMs);
  }
  if (lastError) throw lastError;
  if (!response || !response.ok) {
    throw new Error("Anthropic request failed: no successful response");
  }
  const payload = await response.json();
  const text = (payload.content || []).filter((part) => part.type === "text" && typeof part.text === "string").map((part) => part.text?.trim() || "").join("\n").trim();
  let usage;
  if (payload.usage) {
    const created = payload.usage.cache_creation_input_tokens ?? 0;
    const readFromCache = payload.usage.cache_read_input_tokens ?? 0;
    const input = payload.usage.input_tokens ?? 0;
    const output = payload.usage.output_tokens ?? 0;
    usage = {
      inputTokens: input,
      outputTokens: output,
      cacheCreatedTokens: created,
      cacheReadTokens: readFromCache
    };
    if (created > 0 || readFromCache > 0) {
      console.info(
        `[anthropic-cache] input=${input} output=${output} cache_created=${created} cache_read=${readFromCache}`
      );
    }
  }
  return { text, usage };
}
async function callGoogle(params) {
  const response = await fetch(
    `${GOOGLE_API_URL}/${GOOGLE_MODEL}:generateContent?key=${encodeURIComponent(params.apiKey)}`,
    {
      method: "POST",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        systemInstruction: {
          role: "system",
          parts: [{ text: params.system }]
        },
        contents: params.messages.map((message) => ({
          role: message.role === "assistant" ? "model" : "user",
          parts: [{ text: message.content }]
        })),
        generationConfig: {
          maxOutputTokens: params.maxOutputTokens
        }
      }),
      signal: params.signal
    }
  );
  if (!response.ok) {
    const payload2 = await response.text().catch(() => "");
    throw new Error(
      `Google request failed (${response.status}): ${payload2.slice(0, 240)}`
    );
  }
  const payload = await response.json();
  const text = payload.candidates?.[0]?.content?.parts?.map((part) => part.text || "").join("\n").trim() || "";
  const usage = typeof payload.usageMetadata?.promptTokenCount === "number" && typeof payload.usageMetadata?.candidatesTokenCount === "number" ? {
    inputTokens: payload.usageMetadata.promptTokenCount,
    outputTokens: payload.usageMetadata.candidatesTokenCount
  } : void 0;
  return { text, usage };
}
async function callOpenAiCompatible(params) {
  const response = await fetch(params.apiUrl, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${params.apiKey}`
    },
    body: JSON.stringify({
      model: params.model,
      messages: [
        { role: "system", content: params.system },
        ...params.messages
      ],
      max_tokens: params.maxOutputTokens
    }),
    signal: params.signal
  });
  if (!response.ok) {
    const payload2 = await response.text().catch(() => "");
    throw new Error(
      `Provider request failed (${response.status}): ${payload2.slice(0, 240)}`
    );
  }
  const payload = await response.json();
  const text = payload.choices?.[0]?.message?.content?.trim() || payload.reply?.trim() || payload.output_text?.trim() || "";
  const usage = typeof payload.usage?.prompt_tokens === "number" && typeof payload.usage?.completion_tokens === "number" ? {
    inputTokens: payload.usage.prompt_tokens,
    outputTokens: payload.usage.completion_tokens
  } : void 0;
  return { text, usage };
}
function concatCacheablePrefixIntoFirstUser(messages, cacheableUserPrefix) {
  if (!cacheableUserPrefix) return messages;
  const firstUserIndex = messages.findIndex((m) => m.role === "user");
  if (firstUserIndex === -1) return messages;
  return messages.map(
    (message, idx) => idx === firstUserIndex ? { ...message, content: `${cacheableUserPrefix}

${message.content}` } : message
  );
}
async function executeProvider(params) {
  const maxOutputTokens = params.request.context.maxOutputTokens || 1200;
  let result = { text: "" };
  const collapsedMessages = concatCacheablePrefixIntoFirstUser(
    params.request.messages,
    params.request.cacheableUserPrefix
  );
  const signal = params.request.context.signal;
  if (params.provider === "openai") {
    result = isAnthropicKey(params.apiKey) ? await callAnthropic({
      apiKey: params.apiKey,
      system: params.request.system,
      messages: params.request.messages,
      maxOutputTokens,
      cacheableUserPrefix: params.request.cacheableUserPrefix,
      signal,
      model: params.request.context.modelOverride
    }) : await callOpenAI({
      apiKey: params.apiKey,
      system: params.request.system,
      messages: collapsedMessages,
      maxOutputTokens,
      signal
    });
  } else if (params.provider === "google") {
    result = await callGoogle({
      apiKey: params.apiKey,
      system: params.request.system,
      messages: collapsedMessages,
      maxOutputTokens,
      signal
    });
  } else if (params.provider === "minimax") {
    result = await callOpenAiCompatible({
      apiUrl: MINIMAX_API_URL,
      apiKey: params.apiKey,
      model: MINIMAX_MODEL,
      system: params.request.system,
      messages: collapsedMessages,
      maxOutputTokens,
      signal
    });
  } else {
    result = await callOpenAiCompatible({
      apiUrl: NVIDIA_API_URL,
      apiKey: params.apiKey,
      model: NVIDIA_MODEL,
      system: params.request.system,
      messages: collapsedMessages,
      maxOutputTokens,
      signal
    });
  }
  const output = result.text;
  let inputTokens;
  let outputTokens;
  if (result.usage) {
    inputTokens = result.usage.inputTokens;
    outputTokens = result.usage.outputTokens;
  } else {
    const messageConcat = [
      params.request.system,
      ...collapsedMessages.map((message) => message.content)
    ].join("\n");
    inputTokens = estimateInputTokens(messageConcat);
    outputTokens = estimateOutputTokens(output);
  }
  const estimatedCostUsd = estimateCostUsd({
    provider: params.provider,
    inputTokens,
    outputTokens
  });
  return {
    provider: params.provider,
    output,
    inputTokens,
    outputTokens,
    estimatedCostUsd,
    qualityScore: qualityScore(output),
    using: params.using
  };
}
async function generateWithBrain(request) {
  await ensureHealthHydrated();
  const mode = normalizeProviderMode(request.context.mode);
  const qualityFloor = typeof request.context.qualityFloor === "number" ? request.context.qualityFloor : brainFlags.qualityFloor;
  const availability = providerAvailabilityForUser({
    userKeys: request.context.userKeys,
    platformKeysEnabled: request.context.platformKeysEnabled
  });
  let providerOrder;
  if (mode === "auto") {
    providerOrder = sortClaudeFirstThenCheapest(
      availability.filter((entry) => entry.available).map((entry) => entry.provider)
    );
  } else {
    const forced = availability.find((entry) => entry.provider === mode);
    if (!forced || !forced.available) {
      throw new Error(
        forced?.reason || "Selected brain is currently unavailable."
      );
    }
    providerOrder = [mode];
  }
  if (providerOrder.length === 0) {
    throw new Error("No available brain providers configured.");
  }
  let firstError = null;
  for (const provider of providerOrder) {
    const credentials = providerCredentials(provider, request.context.userKeys);
    const resolved = resolvedApiKey(credentials);
    if (!resolved.key) continue;
    const startedAt = Date.now();
    try {
      const result = await executeProvider({
        provider,
        apiKey: resolved.key,
        using: resolved.using === "byo_key" ? "byo_key" : "platform_key",
        request
      });
      if (result.qualityScore < qualityFloor && mode === "auto") {
        recordProviderHealth({
          provider,
          success: false,
          latencyMs: Date.now() - startedAt
        });
        continue;
      }
      recordProviderHealth({
        provider,
        success: true,
        latencyMs: Date.now() - startedAt
      });
      return result;
    } catch (error) {
      recordProviderHealth({
        provider,
        success: false,
        latencyMs: Date.now() - startedAt
      });
      if (!firstError && error instanceof Error) {
        firstError = error;
      }
      if (mode !== "auto") {
        throw error;
      }
    }
  }
  throw firstError || new Error("All brain providers failed quality/call checks.");
}
var OPENAI_API_URL, ANTHROPIC_API_URL, GOOGLE_API_URL, MINIMAX_API_URL, NVIDIA_API_URL, OPENAI_MODEL, ANTHROPIC_MODEL, GOOGLE_MODEL, MINIMAX_MODEL, NVIDIA_MODEL, providerHealth, healthHydrated, healthHydrationPromise, HEALTH_PERSIST_THROTTLE_MS, lastHealthPersistAt, ANTHROPIC_CACHE_MIN_CHARS, ANTHROPIC_RETRY_MAX_ATTEMPTS, ANTHROPIC_RETRY_BASE_BACKOFF_MS, ANTHROPIC_RETRYABLE_STATUS;
var init_router = __esm({
  "src/lib/brain/router.ts"() {
    "use strict";
    init_cost();
    init_flags();
    init_health_store();
    OPENAI_API_URL = "https://api.openai.com/v1/chat/completions";
    ANTHROPIC_API_URL = "https://api.anthropic.com/v1/messages";
    GOOGLE_API_URL = "https://generativelanguage.googleapis.com/v1beta/models";
    MINIMAX_API_URL = process.env.MINIMAX_API_URL || "https://api.minimax.chat/v1/text/chatcompletion_v2";
    NVIDIA_API_URL = process.env.NVIDIA_KIMI_API_URL || "https://integrate.api.nvidia.com/v1/chat/completions";
    OPENAI_MODEL = process.env.OPENAI_CHAT_MODEL || "gpt-5-nano";
    ANTHROPIC_MODEL = process.env.ANTHROPIC_CHAT_MODEL || "claude-sonnet-4-6";
    GOOGLE_MODEL = process.env.GOOGLE_CHAT_MODEL || "gemini-2.0-flash-lite";
    MINIMAX_MODEL = process.env.MINIMAX_CHAT_MODEL || "MiniMax-M2";
    NVIDIA_MODEL = process.env.NVIDIA_KIMI_MODEL || "moonshotai/kimi-k2-instruct-0905";
    providerHealth = /* @__PURE__ */ new Map();
    healthHydrated = false;
    healthHydrationPromise = null;
    HEALTH_PERSIST_THROTTLE_MS = 3e4;
    lastHealthPersistAt = 0;
    ANTHROPIC_CACHE_MIN_CHARS = 4096;
    ANTHROPIC_RETRY_MAX_ATTEMPTS = 3;
    ANTHROPIC_RETRY_BASE_BACKOFF_MS = 1e3;
    ANTHROPIC_RETRYABLE_STATUS = (status) => status === 429 || status >= 500 && status < 600;
  }
});

// src/lib/soul-audit/grounded-weave.ts
function stripCodeFence(raw) {
  let s = raw.trim();
  if (s.startsWith("```")) {
    s = s.replace(/^```json?\s*/i, "").replace(/\s*```$/, "");
  }
  return s.trim();
}
function derivePreview(body) {
  const plain = body.replace(/[#*_>`]/g, "").replace(/\s+/g, " ").trim();
  const words = plain.split(" ");
  if (words.length <= 120) return plain;
  return words.slice(0, 110).join(" ").replace(/[,;:]$/, "") + "\u2026";
}
function wordStudyToHebrewGreek(w) {
  return {
    word: w.word,
    transliteration: w.xlit,
    language: w.strong.startsWith("G") ? "greek" : "hebrew",
    meaning: w.gloss,
    etymology: w.source
  };
}
async function safeWordStudies(reference, max) {
  try {
    return await getWordStudiesForVerse(reference, max);
  } catch {
    return [];
  }
}
function formatWordStudies(studies) {
  if (studies.length === 0)
    return "(none available for this verse \u2014 do NOT introduce any Hebrew/Greek word claims)";
  return studies.map(
    (w) => `- ${w.word} (${w.xlit}, ${w.strong})${w.englishWord ? ` \u2014 the word behind "${w.englishWord}"` : ""}: ${w.gloss} [${w.source}]`
  ).join("\n");
}
function formatSources(chunks) {
  return chunks.map(
    (c, i) => `[${i + 1}] ${c.attribution}:
"${c.content.replace(/\s+/g, " ").trim().slice(0, 700)}"`
  ).join("\n\n");
}
function escapeRegExp(s) {
  return s.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function injectWordNoteMarkers(body, studies) {
  if (!body || studies.length === 0) return { body, emittedIds: [] };
  const seenIds = /* @__PURE__ */ new Set();
  const candidates = [];
  for (const study of studies) {
    const entry = BANK_BY_STRONG.get((study.strong || "").toUpperCase());
    if (!entry) continue;
    if (seenIds.has(entry.id)) continue;
    const term = (entry.term || "").trim();
    if (!term) continue;
    seenIds.add(entry.id);
    candidates.push({ id: entry.id, term });
  }
  if (candidates.length === 0) return { body, emittedIds: [] };
  const lines = body.split("\n");
  const emittedIds = [];
  candidates.sort((a, b) => b.term.length - a.term.length);
  for (const { id, term } of candidates) {
    const re = new RegExp(`\\b(${escapeRegExp(term)})\\b`, "i");
    let marked = false;
    for (let i = 0; i < lines.length && !marked; i++) {
      const line = lines[i];
      if (line.trimStart().startsWith(">")) continue;
      if (line.includes("{{wn:")) {
        if (re.test(line.replace(/\{\{wn:[^}]*\}\}/g, ""))) {
        } else {
          continue;
        }
      }
      const m = line.match(re);
      if (!m || m.index === void 0) continue;
      const before = line.slice(0, m.index);
      const openMarkers = (before.match(/\{\{wn:/g) || []).length;
      const closeMarkers = (before.match(/\}\}/g) || []).length;
      if (openMarkers > closeMarkers) continue;
      const surface = m[1];
      lines[i] = before + `{{wn:${id}|${surface}}}` + line.slice(m.index + surface.length);
      emittedIds.push(id);
      marked = true;
    }
  }
  return { body: lines.join("\n"), emittedIds };
}
function countWords(s) {
  return s.split(/\s+/).filter(Boolean).length;
}
function hasScriptureBlockquote(body, scriptureText) {
  const quoteLines = body.split("\n").filter((l) => l.trimStart().startsWith(">")).map((l) => l.replace(/^\s*>+\s?/, "").trim()).filter(Boolean);
  if (quoteLines.length === 0) return false;
  const anchorWords = scriptureText.toLowerCase().replace(/[^a-z\s]/g, " ").split(/\s+/).filter((w) => w.length > 2);
  if (anchorWords.length === 0) return quoteLines.length > 0;
  const probe = anchorWords.slice(0, 3).join(" ");
  const quoteWords = quoteLines.join(" ").toLowerCase().replace(/[^a-z\s]/g, " ").split(/\s+/).filter((w) => w.length > 2).join(" ");
  return quoteWords.includes(probe);
}
function checkDepth(params) {
  const issues = [];
  const floor = DEPTH_FLOOR[params.mode];
  const words = countWords(params.body);
  if (words < floor.minWords) {
    issues.push(
      `thin generation: body has ${words} words, below the ${floor.minWords}-word ${params.mode} floor`
    );
  }
  const proseParagraphs = params.body.split(/\n\s*\n/).map((p) => p.trim()).filter(
    (p) => p && !p.split("\n").every((l) => l.trimStart().startsWith(">"))
  );
  if (proseParagraphs.length < 2) {
    issues.push(
      `incoherent structure: only ${proseParagraphs.length} prose paragraph(s) \u2014 expected multi-paragraph woven prose`
    );
  }
  if (floor.requireScriptureQuote && !hasScriptureBlockquote(params.body, params.scriptureText)) {
    issues.push(
      "missing required element: anchor Scripture is not quoted verbatim as a blockquote"
    );
  }
  if (floor.requirePrayer && countWords(params.prayer) < 8) {
    issues.push(
      "missing required element: closing prayer is absent or too short"
    );
  }
  if (floor.minHeadings > 0) {
    const headingCount = (params.body.match(/^#{1,6}\s+\S/gm) ?? []).length;
    if (headingCount < floor.minHeadings) {
      issues.push(
        `thin deep dive: ${headingCount} section heading(s), below the ${floor.minHeadings} expected for long-form structure`
      );
    }
  }
  return issues;
}
function verify(params) {
  const issues = [];
  const low = params.body.toLowerCase();
  for (const author of KNOWN_AUTHORS) {
    if (low.includes(author) && !params.allowedAuthors.has(author)) {
      issues.push(
        `ungrounded author citation: "${author}" (not in retrieved sources)`
      );
    }
  }
  const stripDiacritics = (s) => s.normalize("NFD").replace(/[̀-ͯ]/g, "").toLowerCase().replace(/[^a-z]/g, "");
  const allowedXlit = new Set(
    params.studies.flatMap((w) => [
      w.xlit.toLowerCase(),
      stripDiacritics(w.xlit)
    ])
  );
  const groundedTokens = /* @__PURE__ */ new Set();
  for (const t of params.groundingText.match(/[\p{L}ʼʹ'’-]{2,}/gu) ?? []) {
    const s = stripDiacritics(t);
    if (s.length >= 3) groundedTokens.add(s);
  }
  const tokens = params.body.match(/[\p{L}ʼʹ'’-]{2,}/gu) ?? [];
  for (const tok of tokens) {
    if (![...tok].some((c) => c.charCodeAt(0) > 127)) continue;
    const cand = stripDiacritics(tok);
    if (cand.length < 3) continue;
    const inLexicon = [...allowedXlit].some(
      (x) => x.includes(cand) || cand.includes(x)
    );
    const inGrounding = groundedTokens.has(cand);
    if (!inLexicon && !inGrounding) {
      issues.push(`possibly-ungrounded transliteration: "${tok}"`);
    }
  }
  return { ok: issues.length === 0, issues: [...new Set(issues)] };
}
function buildStructuredEndnotes(params) {
  const endnotes = [];
  let id = 1;
  const low = params.body.toLowerCase();
  endnotes.push({
    id: id++,
    kind: "scripture",
    source: `${params.scriptureReference} (${params.translation})`,
    reference: params.scriptureReference,
    note: "Anchor Scripture, quoted verbatim from the Euangelion corpus."
  });
  for (const s of params.sources) {
    const attribution = (s.source || "").trim();
    if (!attribution) continue;
    const authorSegment = attribution.split(",")[0]?.trim() ?? "";
    if (!authorSegment) continue;
    const distinctive = authorSegment.split(/\s+/).map((w) => w.replace(/[^\p{L}]/gu, "")).filter((w) => w.length >= 4).sort((a, b) => b.length - a.length)[0];
    if (!distinctive) continue;
    if (!low.includes(distinctive.toLowerCase())) continue;
    if (endnotes.some((e) => e.kind === "voice" && e.source === attribution)) {
      continue;
    }
    const excerpt = s.content.replace(/\s+/g, " ").trim().slice(0, 240);
    endnotes.push({
      id: id++,
      kind: "voice",
      source: attribution,
      reference: attribution,
      note: excerpt ? `Quoted in this day's reading from the Euangelion reference library: "${excerpt}${s.content.length > 240 ? "\u2026" : ""}"` : "Quoted in this day's reading from the Euangelion reference library."
    });
  }
  for (const w of params.studies) {
    endnotes.push({
      id: id++,
      kind: "lexicon",
      source: w.source,
      // e.g. "Brown-Driver-Briggs (Strong's H7965)"
      reference: w.strong,
      note: `Word study of ${w.word} (${w.xlit}, ${w.strong}): ${w.gloss}.`
    });
  }
  return endnotes;
}
function systemPrompt() {
  return `You are the assembler-and-polisher for Euangelion, a CLOSED, grounded devotional. Theological frame: historic orthodox Christianity (the Apostles' and Nicene Creeds), Christ-centered, never sectarian or novel.

You write with literary beauty, theological depth, and restraint. But you are an ASSEMBLER, not an inventor.

ABSOLUTE GROUNDING RULES (a closed system to prevent hallucination):
- Quote the provided Scripture verbatim, with its reference. Do not quote any other Scripture passage unless its full text is provided to you.
- Any Hebrew/Greek word claim MUST come from the provided lexicon entries. NEVER invent etymologies, pictographic/letter meanings, or root theories. Use only the glosses given. If no lexicon entries are provided, make no original-language claims at all.
- You may quote ONLY the provided source excerpts, verbatim, attributed to the named author/work. Introduce NO other quotations, authors, or historical claims.
- If you cannot ground a claim in the provided materials, leave it out. Beauty through restraint, never through invention.

OUTPUT FORMAT: return your answer using the exact @@@SECTION@@@ delimiter lines requested \u2014 never JSON, never a code fence. Write each section's content freely (quotes, blockquotes, markdown all welcome) between its delimiter and the next.`;
}
function parseDelimited(raw) {
  const out = {};
  const parts = raw.split(/@@@([A-Z]+)@@@/g);
  for (let i = 1; i < parts.length; i += 2) {
    out[parts[i].toLowerCase()] = (parts[i + 1] ?? "").trim();
  }
  return out;
}
function parseList(block) {
  if (!block) return [];
  return block.split("\n").map((l) => l.replace(/^\s*[-*•\d.]+\s*/, "").trim()).filter(Boolean);
}
function readingUserPrompt(params) {
  return `PERSON'S STRUGGLE:
"${params.struggle}"

THIS IS DAY ${params.dayNumber} OF ${params.totalDays}.${params.previousDaysSummary ? `
SO FAR (previous days, for continuity \u2014 do not repeat): ${params.previousDaysSummary}` : ""}

ANCHOR SCRIPTURE (quote verbatim, attribute as ${params.ref}, ${params.translation}):
"${params.scriptureText}"

PROVIDED LEXICON ENTRIES (the ONLY original-language data you may use):
${params.studiesBlock}

PROVIDED SOURCE EXCERPTS (the ONLY quotations/voices you may use; quote verbatim and attribute):
${params.sourcesBlock}

TASK \u2014 Write Day ${params.dayNumber} as ONE flowing devotional reading of ~950\u20131,150 words. Weave (no rigid section labels): meet the person honestly where they are; bring the anchor Scripture close; open ONE grounded word study using only the lexicon entries above; weave in AT LEAST ONE provided source quote verbatim and attributed; turn toward the nearness/work of God in Christ (grace, not performance); end with a short honest prayer. Use ONLY the materials above \u2014 invent nothing.

Return EXACTLY this delimited format (these literal lines, nothing before @@@TITLE@@@):
@@@TITLE@@@
an evocative title (no "Day N:" prefix)
@@@BODY@@@
the full reading as markdown prose \u2014 quote the verbatim Scripture as a blockquote, attribute the source quote inline
@@@PRAYER@@@
a short first-person prayer
@@@REFLECTION@@@
- 2 to 3 honest, non-leading questions, one per line
@@@SUMMARY@@@
1-2 sentences capturing this day's movement, for the next day's continuity`;
}
function deepDiveUserPrompt(params) {
  return `PERSON'S STRUGGLE:
"${params.struggle}"

ANCHOR SCRIPTURE (verbatim, ${params.ref}, ${params.translation}):
"${params.scriptureText}"

PROVIDED LEXICON ENTRIES (the ONLY original-language data you may use):
${params.studiesBlock}

PROVIDED SOURCE EXCERPTS (the ONLY quotations/voices you may use; verbatim + attributed):
${params.sourcesBlock}

TASK \u2014 Write a long-form "Deep Dive" (~3,000\u20133,800 words) in flowing prose with section headings, in the tradition of a rich theological teaching. Include: an opening that frames the passage; grounded word studies drawn ONLY from the lexicon entries above; a reading through PaRDeS (Peshat / Remez / Derash / Sod \u2014 the four classical levels); canonical cross-references that you may name by reference (book chapter:verse) but only quote if their text is provided; the historic voices woven verbatim and attributed; a Christ-centered typological reading; and a closing invitation. Use ONLY the provided materials for quotations, original-language claims, and attributions. Invent nothing.

Return EXACTLY this delimited format (these literal lines, nothing before @@@BODY@@@):
@@@BODY@@@
the full deep dive as markdown prose with ## section headings
@@@ETYMOLOGY@@@
a paragraph synthesizing the provided word studies (lexicon-grounded only)
@@@CROSSREFS@@@
one per line as: Book C:V | one sentence on the link
@@@JOURNALING@@@
- 3 to 4 prompts, one per line
@@@COMPREHENSION@@@
- 3 to 4 questions, one per line`;
}
async function generateGroundedDay(input) {
  const mode = input.mode ?? "reading";
  const translation = input.translation ?? DEFAULT_BIBLE_TRANSLATION;
  const model = input.modelOverride ?? SONNET;
  const verse = await getVerse(input.scriptureReference, translation);
  const studies = await safeWordStudies(
    verse.canonical,
    mode === "deepdive" ? 4 : 2
  );
  const allChunks = await loadReferenceIndex();
  const query = `${input.struggle} ${input.theme} ${verse.canonical}`;
  const topK = mode === "deepdive" ? 10 : 6;
  const { selected, selectedIds } = selectTopChunks(
    allChunks,
    query,
    input.usedChunkIds ?? [],
    topK,
    0.1,
    4
  );
  const sources = selected.map((c) => ({
    source: c.source,
    content: c.content,
    attribution: attributionFromChunk(c)
  }));
  const allowedAuthors = /* @__PURE__ */ new Set();
  for (const s of sources) {
    const a = s.attribution.toLowerCase();
    for (const known of KNOWN_AUTHORS)
      if (a.includes(known)) allowedAuthors.add(known);
  }
  const studiesBlock = formatWordStudies(studies);
  const sourcesBlock = formatSources(sources);
  const user = mode === "deepdive" ? deepDiveUserPrompt({
    struggle: input.struggle,
    ref: verse.canonical,
    translation,
    scriptureText: verse.text,
    studiesBlock,
    sourcesBlock
  }) : readingUserPrompt({
    struggle: input.struggle,
    ref: verse.canonical,
    translation,
    scriptureText: verse.text,
    studiesBlock,
    sourcesBlock,
    dayNumber: input.dayNumber,
    totalDays: input.totalDays,
    previousDaysSummary: input.previousDaysSummary
  });
  const result = await generateWithBrain({
    system: systemPrompt(),
    messages: [{ role: "user", content: user }],
    context: {
      task: "devotional_day_generate",
      mode: "auto",
      maxOutputTokens: mode === "deepdive" ? 7e3 : 2600,
      modelOverride: model,
      platformKeysEnabled: true,
      signal: input.signal
    }
  });
  const parsed = parseDelimited(stripCodeFence(result.output));
  const body = (parsed.body ?? "").trim();
  if (!body) throw new Error("GROUNDED_WEAVE_EMPTY_BODY");
  const groundingText = [
    verse.text,
    ...sources.map((s) => `${s.attribution} ${s.content}`)
  ].join(" ");
  const verification = verify({
    body,
    scriptureText: verse.text,
    allowedAuthors,
    studies,
    groundingText
  });
  const depthIssues = checkDepth({
    body,
    prayer: (parsed.prayer ?? "").trim(),
    mode,
    scriptureText: verse.text
  });
  if (depthIssues.length > 0) {
    verification.issues.push(...depthIssues);
    verification.ok = false;
  }
  const { body: markedBody, emittedIds: wordNoteIds } = injectWordNoteMarkers(
    body,
    studies
  );
  const endnotes = buildStructuredEndnotes({
    body,
    scriptureReference: verse.canonical,
    translation,
    sources,
    studies
  });
  const primaryStudy = studies[0] ? wordStudyToHebrewGreek(studies[0]) : null;
  let tier3Extended = null;
  if (mode === "deepdive") {
    const crossRefs = parseList(parsed.crossrefs).map((line) => {
      const [reference, connection] = line.split("|").map((s) => s.trim());
      return { reference: reference ?? "", connection: connection ?? "" };
    });
    tier3Extended = {
      extendedEtymology: parsed.etymology ?? "",
      crossReferences: crossRefs,
      journalingPrompts: parseList(parsed.journaling),
      comprehensionQuestions: parseList(parsed.comprehension),
      characterProfile: null,
      furtherResources: sources.slice(0, 5).map((s) => ({
        type: "commentary",
        title: s.attribution,
        author: s.attribution.split(",")[0] ?? "",
        note: "From the Euangelion reference library."
      }))
    };
  }
  const content = {
    title: parsed.title || verse.canonical,
    hookA: "",
    textB: markedBody,
    textBPreview: derivePreview(body),
    centerC: "",
    christConnectionBPrime: "",
    returnAPrime: "",
    scriptureReference: verse.canonical,
    scriptureText: verse.text,
    // verbatim from corpus
    hebrewGreekStudy: primaryStudy,
    interactiveElement: { type: "reflection", content: "" },
    metaStoryPlacement: "",
    backwardLink: "",
    forwardLink: "",
    reflectionQuestions: parseList(parsed.reflection),
    prayer: parsed.prayer ?? "",
    endnotes,
    previousDaysSummaryForNext: parsed.summary ?? "",
    tier3Extended
  };
  const words = body.split(/\s+/).filter(Boolean).length;
  return {
    content,
    usedChunkIds: selectedIds,
    verification,
    meta: {
      mode,
      model,
      words,
      scriptureTranslation: translation,
      wordStudyCount: studies.length,
      wordNoteCount: wordNoteIds.length,
      sourceCount: sources.length,
      endnoteCount: endnotes.length,
      // Real provider usage from this call (the brain router / edge shim both
      // populate these from the Anthropic response). The runner ledgers them.
      inputTokens: result.inputTokens,
      outputTokens: result.outputTokens,
      estimatedCostUsd: result.estimatedCostUsd
    }
  };
}
var SONNET, KNOWN_AUTHORS, BANK_BY_STRONG, DEPTH_FLOOR;
var init_grounded_weave = __esm({
  "src/lib/soul-audit/grounded-weave.ts"() {
    "use strict";
    init_getVerse();
    init_translations();
    init_reference_index_loader();
    init_chunk_retrieval();
    init_lexicon();
    init_wordnote_bank();
    init_router();
    SONNET = "claude-sonnet-4-6";
    KNOWN_AUTHORS = [
      "augustine",
      "calvin",
      "pascal",
      "luther",
      "kempis",
      "spurgeon",
      "edwards",
      "bunyan",
      "wesley",
      "chesterton",
      "owen",
      "murray",
      "tozer",
      "bounds",
      "whitefield",
      "whitall smith",
      "douglass",
      "aquinas",
      "lewis",
      "bonhoeffer",
      "nouwen",
      "merton",
      "wright",
      "piper",
      "keller",
      "stott"
    ];
    BANK_BY_STRONG = new Map(
      WORDNOTE_BANK.map((e) => [e.strong.toUpperCase(), e])
    );
    DEPTH_FLOOR = {
      reading: {
        minWords: 600,
        // A complete reading must quote the anchor Scripture as a blockquote and
        // close with a prayer (both are required elements of the woven format).
        requireScriptureQuote: true,
        requirePrayer: true,
        minHeadings: 0
      },
      deepdive: {
        minWords: 1800,
        requireScriptureQuote: false,
        // deep dive may reference more than it quotes
        requirePrayer: false,
        minHeadings: 3
        // the deep-dive prompt asks for ## section headings
      }
    };
  }
});

// src/lib/soul-audit/plan-composition.ts
function composeRecap(savedDays, planTheme) {
  const textB = savedDays.filter((d) => d.day_number <= 5).map((d) => {
    const c = d.content;
    const summary = c.previousDaysSummaryForNext || `${c.title} \u2014 ${c.scriptureReference}`;
    return `**Day ${d.day_number}: ${c.title}** \u2014 ${c.scriptureReference}
${summary}`;
  }).join("\n\n");
  return {
    title: "Week in Review",
    hookA: `This week you explored "${planTheme}." Here's what God revealed.`,
    textB,
    textBPreview: `A reflection on your five-day journey through ${planTheme}.`,
    centerC: "",
    christConnectionBPrime: "",
    returnAPrime: "Review your journal entries, revisit a day that struck you, or sit in what you've received.",
    scriptureReference: "",
    scriptureText: "",
    hebrewGreekStudy: null,
    interactiveElement: {
      type: "weekly_review",
      content: "Which day challenged you most? Which gave you peace?"
    },
    metaStoryPlacement: "",
    backwardLink: `This week covered days 1-5 of ${planTheme}.`,
    forwardLink: "Tomorrow is Sabbath. Rest.",
    reflectionQuestions: [
      "Which scripture stayed with you?",
      "What shifted in your understanding?",
      "What will you carry forward?"
    ],
    prayer: "Lord, thank you for this week of walking with Your word. Seal what You have spoken. Let it bear fruit in the days ahead. Amen.",
    endnotes: [],
    previousDaysSummaryForNext: "",
    tier3Extended: null
  };
}
var SABBATH_DAY;
var init_plan_composition = __esm({
  "src/lib/soul-audit/plan-composition.ts"() {
    "use strict";
    SABBATH_DAY = {
      title: "Sabbath Rest",
      hookA: "Today is Sabbath. No new content. Just rest.\n\nYou spent five days in the Word and one in review. Now be still.",
      textB: '"Be still, and know that I am God; I will be exalted among the nations." \u2014 Psalm 46:10',
      textBPreview: "Be still, and know that I am God. \u2014 Psalm 46:10",
      centerC: "",
      christConnectionBPrime: "",
      returnAPrime: "",
      scriptureReference: "Psalm 46:10",
      scriptureText: "Be still, and know that I am God; I will be exalted among the nations, I will be exalted in the earth.",
      hebrewGreekStudy: null,
      interactiveElement: {
        type: "sabbath_silence",
        content: "Set a timer for 5 minutes. Sit in silence. You are held."
      },
      metaStoryPlacement: "",
      backwardLink: "This week you walked through five days of devotion and one of review.",
      forwardLink: "",
      reflectionQuestions: [
        "What moment from this week do you most want to carry into the next?"
      ],
      prayer: "",
      endnotes: [],
      previousDaysSummaryForNext: "",
      tier3Extended: null
    };
  }
});

// src/lib/supabase/admin.ts
import { createClient } from "@supabase/supabase-js";
function createAdminClient() {
  if (adminClient) return adminClient;
  adminClient = createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL,
    process.env.SUPABASE_SERVICE_ROLE_KEY,
    {
      auth: {
        autoRefreshToken: false,
        persistSession: false
      }
    }
  );
  return adminClient;
}
var adminClient;
var init_admin = __esm({
  "src/lib/supabase/admin.ts"() {
    "use strict";
    adminClient = null;
  }
});

// src/lib/soul-audit/telemetry.ts
function emitSoulAuditTelemetry(event, fields = {}) {
  try {
    const { sessionToken, ...rest } = fields;
    const payload = {
      scope: "soul-audit",
      event,
      ts: (/* @__PURE__ */ new Date()).toISOString(),
      ...rest,
      // Truncate the session token in logs — enough to correlate, not the full
      // secret cookie value.
      ...sessionToken ? { sessionToken: sessionToken.slice(0, 12) } : {}
    };
    const line = `[soul-audit:telemetry] ${JSON.stringify(payload)}`;
    if (event === "generation_fail" || event === "rate_limited" || event === "budget_exceeded" || event === "ledger_write_failed") {
      console.error(line);
    } else {
      console.info(line);
    }
  } catch {
  }
}
var init_telemetry = __esm({
  "src/lib/soul-audit/telemetry.ts"() {
    "use strict";
  }
});

// src/lib/soul-audit/budget-cap.ts
function toFloat(value) {
  if (value === void 0 || value.trim() === "") return null;
  const n = Number(value);
  return Number.isFinite(n) && n >= 0 ? n : null;
}
function dailyCostBudgetUsd() {
  return toFloat(process.env.SOUL_AUDIT_DAILY_COST_BUDGET) ?? DEFAULT_DAILY_COST_BUDGET_USD;
}
function dailyTokenBudget() {
  return toFloat(process.env.SOUL_AUDIT_DAILY_TOKEN_BUDGET);
}
function utcDay(now = /* @__PURE__ */ new Date()) {
  return now.toISOString().slice(0, 10);
}
function maybeAdminClient() {
  if (!process.env.NEXT_PUBLIC_SUPABASE_URL || !process.env.SUPABASE_SERVICE_ROLE_KEY) {
    return null;
  }
  try {
    return createAdminClient();
  } catch {
    return null;
  }
}
async function getDailySpend(now = /* @__PURE__ */ new Date()) {
  const supabase = maybeAdminClient();
  if (!supabase) {
    return { spendUsd: 0, tokens: 0, degraded: true };
  }
  try {
    const { data, error } = await supabase.from("soul_audit_daily_counters").select("spend_usd, input_tokens, output_tokens").eq("utc_day", utcDay(now)).eq("scope", "global_spend").eq("subject", "global").maybeSingle();
    if (error) {
      emitSoulAuditTelemetry("budget_exceeded", {
        reason: `spend_read_error: ${error.message}`
      });
      return { spendUsd: 0, tokens: 0, degraded: true };
    }
    if (!data) return { spendUsd: 0, tokens: 0, degraded: false };
    const spendUsd = Number(data.spend_usd ?? 0);
    const tokens = Number(data.input_tokens ?? 0) + Number(data.output_tokens ?? 0);
    return {
      spendUsd: Number.isFinite(spendUsd) ? spendUsd : 0,
      tokens: Number.isFinite(tokens) ? tokens : 0,
      degraded: false
    };
  } catch (err) {
    emitSoulAuditTelemetry("budget_exceeded", {
      reason: `spend_read_threw: ${err instanceof Error ? err.message : String(err)}`
    });
    return { spendUsd: 0, tokens: 0, degraded: true };
  }
}
async function checkDailyBudget(now = /* @__PURE__ */ new Date()) {
  const costCeilingUsd = dailyCostBudgetUsd();
  const tokenCeiling = dailyTokenBudget();
  const spend = await getDailySpend(now);
  if (spend.degraded) {
    return {
      ok: true,
      spendUsd: spend.spendUsd,
      tokens: spend.tokens,
      costCeilingUsd,
      tokenCeiling,
      degraded: true
    };
  }
  if (spend.spendUsd >= costCeilingUsd) {
    emitSoulAuditTelemetry("budget_exceeded", {
      reason: "cost_budget_exceeded",
      observed: spend.spendUsd,
      ceiling: costCeilingUsd
    });
    return {
      ok: false,
      reason: "cost_budget_exceeded",
      spendUsd: spend.spendUsd,
      tokens: spend.tokens,
      costCeilingUsd,
      tokenCeiling,
      degraded: false
    };
  }
  if (tokenCeiling !== null && spend.tokens >= tokenCeiling) {
    emitSoulAuditTelemetry("budget_exceeded", {
      reason: "token_budget_exceeded",
      observed: spend.tokens,
      ceiling: tokenCeiling
    });
    return {
      ok: false,
      reason: "token_budget_exceeded",
      spendUsd: spend.spendUsd,
      tokens: spend.tokens,
      costCeilingUsd,
      tokenCeiling,
      degraded: false
    };
  }
  return {
    ok: true,
    spendUsd: spend.spendUsd,
    tokens: spend.tokens,
    costCeilingUsd,
    tokenCeiling,
    degraded: false
  };
}
var DEFAULT_DAILY_COST_BUDGET_USD, BUDGET_PAUSED_MESSAGE;
var init_budget_cap = __esm({
  "src/lib/soul-audit/budget-cap.ts"() {
    "use strict";
    init_admin();
    init_telemetry();
    DEFAULT_DAILY_COST_BUDGET_USD = 25;
    BUDGET_PAUSED_MESSAGE = "Devotional generation is paused for today. We limit how much we generate each day so this stays free and sustainable \u2014 please come back tomorrow.";
  }
});

// src/lib/soul-audit/cost-ledger.ts
function toFloat2(value, fallback) {
  const n = Number(value);
  return Number.isFinite(n) && n >= 0 ? n : fallback;
}
function anthropicInputPerMillionUsd() {
  return toFloat2(process.env.SOUL_AUDIT_INPUT_USD_PER_MTOK, 3);
}
function anthropicOutputPerMillionUsd() {
  return toFloat2(process.env.SOUL_AUDIT_OUTPUT_USD_PER_MTOK, 15);
}
function estimateGenerationCostUsd(params) {
  const input = Math.max(0, params.inputTokens) / 1e6 * anthropicInputPerMillionUsd();
  const output = Math.max(0, params.outputTokens) / 1e6 * anthropicOutputPerMillionUsd();
  return Number((input + output).toFixed(6));
}
function utcDay2(now = /* @__PURE__ */ new Date()) {
  return now.toISOString().slice(0, 10);
}
function maybeAdminClient2() {
  if (!process.env.NEXT_PUBLIC_SUPABASE_URL || !process.env.SUPABASE_SERVICE_ROLE_KEY) {
    return null;
  }
  try {
    return createAdminClient();
  } catch {
    return null;
  }
}
async function recordGenerationUsage(usage) {
  const estimatedCostUsd = estimateGenerationCostUsd({
    inputTokens: usage.inputTokens,
    outputTokens: usage.outputTokens
  });
  const supabase = maybeAdminClient2();
  if (!supabase) {
    emitSoulAuditTelemetry("ledger_write_failed", {
      model: usage.model,
      dayNumber: usage.dayNumber,
      mode: usage.dayMode,
      inputTokens: usage.inputTokens,
      outputTokens: usage.outputTokens,
      estimatedCostUsd,
      reason: "supabase_unconfigured"
    });
    return estimatedCostUsd;
  }
  const day = utcDay2();
  try {
    const { error } = await supabase.from("soul_audit_cost_ledger").insert({
      utc_day: day,
      session_token: usage.sessionToken ?? null,
      job_id: usage.jobId ?? null,
      plan_token: usage.planToken ?? null,
      run_id: usage.runId ?? null,
      day_number: usage.dayNumber ?? null,
      day_mode: usage.dayMode,
      model: usage.model,
      input_tokens: Math.max(0, Math.round(usage.inputTokens)),
      output_tokens: Math.max(0, Math.round(usage.outputTokens)),
      estimated_cost_usd: estimatedCostUsd
    });
    if (error) {
      emitSoulAuditTelemetry("ledger_write_failed", {
        model: usage.model,
        dayNumber: usage.dayNumber,
        mode: usage.dayMode,
        estimatedCostUsd,
        reason: `ledger_insert_error: ${error.message}`
      });
    }
  } catch (err) {
    emitSoulAuditTelemetry("ledger_write_failed", {
      model: usage.model,
      dayNumber: usage.dayNumber,
      mode: usage.dayMode,
      estimatedCostUsd,
      reason: `ledger_insert_threw: ${err instanceof Error ? err.message : String(err)}`
    });
  }
  try {
    const { error } = await supabase.rpc("soul_audit_bump_counter", {
      p_utc_day: day,
      p_scope: "global_spend",
      p_subject: "global",
      p_spend_delta: estimatedCostUsd,
      p_input: Math.max(0, Math.round(usage.inputTokens)),
      p_output: Math.max(0, Math.round(usage.outputTokens))
    });
    if (error) {
      emitSoulAuditTelemetry("ledger_write_failed", {
        reason: `spend_counter_error: ${error.message}`,
        estimatedCostUsd
      });
    }
  } catch (err) {
    emitSoulAuditTelemetry("ledger_write_failed", {
      reason: `spend_counter_threw: ${err instanceof Error ? err.message : String(err)}`,
      estimatedCostUsd
    });
  }
  return estimatedCostUsd;
}
var init_cost_ledger = __esm({
  "src/lib/soul-audit/cost-ledger.ts"() {
    "use strict";
    init_admin();
    init_telemetry();
  }
});

// src/lib/soul-audit/generation-runner.ts
var generation_runner_exports = {};
__export(generation_runner_exports, {
  runDeepDive: () => runDeepDive,
  runGenerationDay: () => runGenerationDay
});
async function updateJob(jobId, fields) {
  const supabase = createAdminClient();
  const { error } = await supabase.from("soul_audit_jobs").update({ ...fields, updated_at: (/* @__PURE__ */ new Date()).toISOString() }).eq("id", jobId);
  if (error) {
    console.error(
      `[generation-runner] Failed to update job ${jobId}:`,
      error.message
    );
  }
}
async function runGenerationDay(job) {
  const { jobId, planId, runId, dayNumber } = job;
  const contentDays = job.totalContentDays || 5;
  const startedAt = Date.now();
  try {
    const budget = await checkDailyBudget();
    if (!budget.ok) {
      emitSoulAuditTelemetry("budget_exceeded", {
        dayNumber,
        jobId,
        planToken: planId,
        sessionToken: job.sessionId,
        reason: budget.reason,
        observed: budget.spendUsd,
        ceiling: budget.costCeilingUsd
      });
      await updateJob(jobId, {
        status: "error",
        error: BUDGET_PAUSED_MESSAGE,
        generating_since: null
      }).catch(() => {
      });
      return { ok: false, status: 429, error: BUDGET_PAUSED_MESSAGE };
    }
    await updateJob(jobId, {
      status: "generating",
      progress: `Composing day ${dayNumber} of 7...`,
      generating_since: (/* @__PURE__ */ new Date()).toISOString()
    });
    emitSoulAuditTelemetry("generation_start", {
      mode: "reading",
      dayNumber,
      jobId,
      planToken: planId,
      sessionToken: job.sessionId
    });
    let weave;
    try {
      weave = await generateGroundedDay({
        struggle: job.userInput,
        scriptureReference: job.scriptureAnchor || "",
        theme: job.theme || "",
        dayNumber,
        totalDays: 7,
        previousDaysSummary: job.previousDaysSummary || "",
        usedChunkIds: job.usedChunkIds || [],
        mode: "reading",
        modelOverride: process.env.SOUL_AUDIT_MODEL || void 0,
        signal: job.signal
      });
    } catch (error) {
      const msg = error instanceof Error ? error.message : "grounded weave failed";
      console.error(
        `[generation-runner] Grounded weave failed for day ${dayNumber}:`,
        msg
      );
      await updateJob(jobId, {
        status: "error",
        error: `Day ${dayNumber} generation failed: ${msg}`,
        generating_since: null
      }).catch(() => {
      });
      const isAbort = error instanceof Error && (error.name === "AbortError" || msg.includes("aborted"));
      emitSoulAuditTelemetry("generation_fail", {
        mode: "reading",
        dayNumber,
        jobId,
        planToken: planId,
        sessionToken: job.sessionId,
        durationMs: Date.now() - startedAt,
        reason: `weave_failed: ${msg}`
      });
      return {
        ok: false,
        status: isAbort ? 504 : 500,
        error: `Day ${dayNumber} generation failed: ${msg}`
      };
    }
    await recordGenerationUsage({
      model: weave.meta.model,
      inputTokens: weave.meta.inputTokens,
      outputTokens: weave.meta.outputTokens,
      dayMode: "reading",
      sessionToken: job.sessionId,
      jobId,
      planToken: planId,
      runId,
      dayNumber
    });
    if (!weave.verification.ok) {
      const issues = weave.verification.issues.join("; ");
      console.error(
        `[generation-runner] Day ${dayNumber} failed verification (grounding/depth):`,
        issues
      );
      emitSoulAuditTelemetry("generation_fail", {
        mode: "reading",
        dayNumber,
        jobId,
        planToken: planId,
        sessionToken: job.sessionId,
        model: weave.meta.model,
        inputTokens: weave.meta.inputTokens,
        outputTokens: weave.meta.outputTokens,
        durationMs: Date.now() - startedAt,
        reason: `verification_failed: ${issues}`
      });
      await updateJob(jobId, {
        status: "error",
        error: `Day ${dayNumber} failed verification: ${issues}`,
        generating_since: null
      });
      return {
        ok: false,
        status: 500,
        error: `verification failed: ${issues}`
      };
    }
    const parsed = weave.content;
    console.info(
      `[generation-runner] Day ${dayNumber} grounded: ${weave.meta.words}w \xB7 ${weave.meta.sourceCount} sources \xB7 ${weave.meta.wordStudyCount} word studies \xB7 ${weave.meta.endnoteCount} endnotes \xB7 ${weave.meta.model} \xB7 verified`
    );
    const supabase = createAdminClient();
    const db = supabase;
    const { error: upsertError } = await db.from("devotional_plan_days").upsert(
      {
        plan_token: planId,
        day_number: dayNumber,
        content: parsed,
        used_chunk_ids: weave.usedChunkIds,
        run_id: runId
      },
      { onConflict: "plan_token,day_number" }
    );
    if (upsertError) {
      console.error(
        `[generation-runner] Upsert failed for day ${dayNumber}:`,
        upsertError.message
      );
      await updateJob(jobId, {
        status: "error",
        error: `Day ${dayNumber} save failed: ${upsertError.message}`,
        generating_since: null
      });
      return { ok: false, status: 500, error: `Day ${dayNumber} save failed` };
    }
    await updateJob(jobId, {
      current_day: dayNumber,
      progress: `Day ${dayNumber} of 7 complete.`
    });
    emitSoulAuditTelemetry("generation_success", {
      mode: "reading",
      dayNumber,
      jobId,
      planToken: planId,
      sessionToken: job.sessionId,
      model: weave.meta.model,
      inputTokens: weave.meta.inputTokens,
      outputTokens: weave.meta.outputTokens,
      durationMs: Date.now() - startedAt
    });
    if (dayNumber >= contentDays) {
      const { data: savedDays } = await db.from("devotional_plan_days").select("day_number, content").eq("plan_token", planId).order("day_number");
      const typedSavedDays = (savedDays || []).map((d) => ({
        day_number: d.day_number,
        content: d.content
      }));
      const recapContent = composeRecap(
        typedSavedDays,
        job.theme || "your journey"
      );
      const { error: recapError } = await db.from("devotional_plan_days").upsert(
        {
          plan_token: planId,
          day_number: 6,
          content: recapContent,
          used_chunk_ids: [],
          run_id: runId
        },
        { onConflict: "plan_token,day_number" }
      );
      if (recapError) {
        console.error(
          "[generation-runner] Recap upsert failed:",
          recapError.message
        );
        await updateJob(jobId, {
          status: "error",
          error: `Recap day save failed: ${recapError.message}`,
          generating_since: null
        });
        return { ok: false, status: 500, error: "Recap day save failed" };
      }
      const { error: sabbathError } = await db.from("devotional_plan_days").upsert(
        {
          plan_token: planId,
          day_number: 7,
          content: SABBATH_DAY,
          used_chunk_ids: [],
          run_id: runId
        },
        { onConflict: "plan_token,day_number" }
      );
      if (sabbathError) {
        console.error(
          "[generation-runner] Sabbath upsert failed:",
          sabbathError.message
        );
        await updateJob(jobId, {
          status: "error",
          error: `Sabbath day save failed: ${sabbathError.message}`,
          generating_since: null
        });
        return { ok: false, status: 500, error: "Sabbath day save failed" };
      }
      await updateJob(jobId, {
        status: "complete",
        current_day: 7,
        progress: "All 7 days generated.",
        generating_since: null
      });
      console.info(
        `[generation-runner] Plan ${planId} complete \u2014 all 7 days generated.`
      );
      return { ok: true, complete: true, nextDay: null, chain: null };
    }
    console.info(
      `[generation-runner] Day ${dayNumber} saved for plan ${planId}.`
    );
    const summarySoFar = [
      job.previousDaysSummary || "",
      `Day ${dayNumber}: ${parsed.previousDaysSummaryForNext || parsed.title}`
    ].filter(Boolean).join(" ");
    return {
      ok: true,
      complete: false,
      nextDay: dayNumber + 1,
      chain: {
        previousDaysSummary: summarySoFar,
        usedChunkIds: [...job.usedChunkIds || [], ...weave.usedChunkIds]
      }
    };
  } catch (error) {
    const message = error instanceof Error ? error.message : "Unknown generation error";
    console.error(
      `[generation-runner] Fatal error on day ${dayNumber} for job ${jobId}:`,
      message
    );
    await updateJob(jobId, {
      status: "error",
      error: `Day ${dayNumber} failed: ${message}`,
      generating_since: null
    }).catch(
      (updateErr) => console.error(
        "[generation-runner] Failed to record error state:",
        updateErr
      )
    );
    return { ok: false, status: 500, error: message };
  }
}
async function runDeepDive(job) {
  const supabase = createAdminClient();
  const startedAt = Date.now();
  const db = supabase;
  const { data: dayRow, error: dayError } = await db.from("devotional_plan_days").select("day_number, content, used_chunk_ids").eq("plan_token", job.planId).eq("day_number", job.dayNumber).single();
  if (dayError || !dayRow) {
    return {
      ok: false,
      status: 404,
      error: `Day ${job.dayNumber} not found for plan.`
    };
  }
  const budget = await checkDailyBudget();
  if (!budget.ok) {
    emitSoulAuditTelemetry("budget_exceeded", {
      mode: "deepdive",
      dayNumber: job.dayNumber,
      planToken: job.planId,
      reason: budget.reason,
      observed: budget.spendUsd,
      ceiling: budget.costCeilingUsd
    });
    return { ok: false, status: 429, error: BUDGET_PAUSED_MESSAGE };
  }
  const existing = dayRow.content;
  emitSoulAuditTelemetry("generation_start", {
    mode: "deepdive",
    dayNumber: job.dayNumber,
    planToken: job.planId
  });
  let weave;
  try {
    weave = await generateGroundedDay({
      struggle: job.userInput,
      scriptureReference: job.scriptureAnchor || existing.scriptureReference || "",
      theme: job.theme || "",
      dayNumber: job.dayNumber,
      totalDays: 7,
      usedChunkIds: dayRow.used_chunk_ids || [],
      mode: "deepdive",
      modelOverride: process.env.SOUL_AUDIT_MODEL || void 0,
      signal: job.signal
    });
  } catch (error) {
    const msg = error instanceof Error ? error.message : "deep dive failed";
    console.error(
      `[generation-runner] Deep dive failed for day ${job.dayNumber}:`,
      msg
    );
    emitSoulAuditTelemetry("generation_fail", {
      mode: "deepdive",
      dayNumber: job.dayNumber,
      planToken: job.planId,
      durationMs: Date.now() - startedAt,
      reason: `weave_failed: ${msg}`
    });
    return { ok: false, status: 500, error: msg };
  }
  await recordGenerationUsage({
    model: weave.meta.model,
    inputTokens: weave.meta.inputTokens,
    outputTokens: weave.meta.outputTokens,
    dayMode: "deepdive",
    planToken: job.planId,
    dayNumber: job.dayNumber
  });
  if (!weave.verification.ok) {
    const issues = weave.verification.issues.join("; ");
    console.error(
      `[generation-runner] Deep dive day ${job.dayNumber} failed verification (grounding/depth):`,
      issues
    );
    emitSoulAuditTelemetry("generation_fail", {
      mode: "deepdive",
      dayNumber: job.dayNumber,
      planToken: job.planId,
      model: weave.meta.model,
      inputTokens: weave.meta.inputTokens,
      outputTokens: weave.meta.outputTokens,
      durationMs: Date.now() - startedAt,
      reason: `verification_failed: ${issues}`
    });
    return { ok: false, status: 500, error: `verification failed: ${issues}` };
  }
  const tier3 = {
    ...weave.content.tier3Extended ?? {
      extendedEtymology: "",
      crossReferences: [],
      journalingPrompts: [],
      comprehensionQuestions: [],
      characterProfile: null,
      furtherResources: []
    },
    deepDiveBody: weave.content.textB
  };
  const merged = { ...existing, tier3Extended: tier3 };
  const { error: saveError } = await db.from("devotional_plan_days").upsert(
    {
      plan_token: job.planId,
      day_number: job.dayNumber,
      content: merged,
      used_chunk_ids: dayRow.used_chunk_ids ?? []
    },
    { onConflict: "plan_token,day_number" }
  );
  if (saveError) {
    return { ok: false, status: 500, error: saveError.message };
  }
  console.info(
    `[generation-runner] Deep dive saved for plan ${job.planId} day ${job.dayNumber}: ${weave.meta.words}w \xB7 ${weave.meta.sourceCount} sources \xB7 verified`
  );
  emitSoulAuditTelemetry("generation_success", {
    mode: "deepdive",
    dayNumber: job.dayNumber,
    planToken: job.planId,
    model: weave.meta.model,
    inputTokens: weave.meta.inputTokens,
    outputTokens: weave.meta.outputTokens,
    durationMs: Date.now() - startedAt
  });
  return { ok: true };
}
var init_generation_runner = __esm({
  "src/lib/soul-audit/generation-runner.ts"() {
    "use strict";
    init_grounded_weave();
    init_plan_composition();
    init_admin();
    init_budget_cap();
    init_cost_ledger();
    init_telemetry();
  }
});

// supabase/functions/generate-plan-day/entry.ts
try {
  const denoEnv = Deno.env.toObject();
  const g = globalThis;
  if (!g.process) g.process = { env: { ...denoEnv } };
  else if (!g.process.env) g.process.env = { ...denoEnv };
  const pe = g.process.env;
  const ensure = (k, v) => {
    try {
      if (v !== void 0 && (pe[k] === void 0 || pe[k] === "")) pe[k] = v;
    } catch {
    }
  };
  for (const [k, v] of Object.entries(denoEnv)) ensure(k, v);
  ensure(
    "NEXT_PUBLIC_SUPABASE_URL",
    pe.NEXT_PUBLIC_SUPABASE_URL || pe.SUPABASE_URL || denoEnv.SUPABASE_URL
  );
} catch (err) {
  console.error("[generate-plan-day] env shim warning:", err);
}
function selfUrl() {
  const base = Deno.env.get("SUPABASE_URL") ?? "";
  return `${base}/functions/v1/generate-plan-day`;
}
function selfHeaders() {
  return {
    "content-type": "application/json",
    "X-Internal-Secret": Deno.env.get("INTERNAL_ROUTE_SECRET") ?? "",
    Authorization: `Bearer ${Deno.env.get("SUPABASE_SERVICE_ROLE_KEY") ?? ""}`
  };
}
async function chainNextDay(job, nextDay, chain) {
  const res = await fetch(selfUrl(), {
    method: "POST",
    headers: selfHeaders(),
    body: JSON.stringify({
      ...job,
      dayNumber: nextDay,
      previousDaysSummary: chain.previousDaysSummary,
      usedChunkIds: chain.usedChunkIds
    })
  });
  if (!res.ok) {
    console.error(
      `[generate-plan-day] chain to day ${nextDay} failed: ${res.status}`
    );
  }
}
Deno.serve(async (req) => {
  if (req.method !== "POST") {
    return new Response(JSON.stringify({ error: "Method not allowed" }), {
      status: 405,
      headers: { "content-type": "application/json" }
    });
  }
  const secret = Deno.env.get("INTERNAL_ROUTE_SECRET") ?? "";
  if (!secret || req.headers.get("X-Internal-Secret") !== secret) {
    return new Response(JSON.stringify({ error: "Forbidden" }), {
      status: 403,
      headers: { "content-type": "application/json" }
    });
  }
  let body;
  try {
    body = await req.json();
  } catch {
    return new Response(JSON.stringify({ error: "Invalid JSON body" }), {
      status: 400,
      headers: { "content-type": "application/json" }
    });
  }
  const { runGenerationDay: runGenerationDay2, runDeepDive: runDeepDive2 } = await Promise.resolve().then(() => (init_generation_runner(), generation_runner_exports));
  if (body.mode === "deepdive") {
    const result2 = await runDeepDive2({
      planId: String(body.planId ?? ""),
      dayNumber: Number(body.dayNumber ?? 0),
      theme: String(body.theme ?? ""),
      scriptureAnchor: String(body.scriptureAnchor ?? ""),
      userInput: String(body.userInput ?? "")
    });
    return new Response(JSON.stringify(result2), {
      status: result2.ok ? 200 : result2.status,
      headers: { "content-type": "application/json" }
    });
  }
  const result = await runGenerationDay2({
    jobId: String(body.jobId ?? ""),
    planId: String(body.planId ?? ""),
    runId: String(body.runId ?? ""),
    dayNumber: Number(body.dayNumber ?? 0),
    totalContentDays: Number(body.totalContentDays ?? 5),
    theme: String(body.theme ?? ""),
    scriptureAnchor: String(body.scriptureAnchor ?? ""),
    userInput: String(body.userInput ?? ""),
    previousDaysSummary: String(body.previousDaysSummary ?? ""),
    usedChunkIds: Array.isArray(body.usedChunkIds) ? body.usedChunkIds : [],
    sessionId: body.sessionId ? String(body.sessionId) : void 0
  });
  if (result.ok && !result.complete && result.nextDay && result.chain) {
    const continuation = chainNextDay(body, result.nextDay, result.chain);
    if (typeof EdgeRuntime !== "undefined") {
      EdgeRuntime.waitUntil(continuation);
    } else {
      void continuation;
    }
  }
  return new Response(JSON.stringify(result), {
    status: result.ok ? 200 : result.status,
    headers: { "content-type": "application/json" }
  });
});
